---
phase: 30-water-gas-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/screens/water_screen.dart
  - test/screens/water_screen_test.dart
autonomous: true

must_haves:
  truths:
    - "Water analytics Analyse tab displays MonthSelector for month-based navigation"
    - "Water analytics Analyse tab displays MonthlySummaryCard with total m3 and % change vs previous month"
    - "Water analytics Analyse tab displays MonthlyBarChart with highlighted selected month"
    - "Water analytics Analyse tab conditionally displays YearComparisonChart and HouseholdComparisonChart"
    - "Old year-based _YearNavigationHeader and _YearlySummaryCard are removed (dead code eliminated)"
  artifacts:
    - path: "lib/screens/water_screen.dart"
      provides: "Month-based Analyse tab using shared widgets"
      contains: "MonthSelector"
    - path: "test/screens/water_screen_test.dart"
      provides: "Updated tests verifying new Analyse tab widgets"
      contains: "MonthSelector"
  key_links:
    - from: "lib/screens/water_screen.dart"
      to: "AnalyticsProvider"
      via: "setSelectedMonth in initState and onMonthChanged"
      pattern: "setSelectedMonth"
    - from: "lib/screens/water_screen.dart"
      to: "MonthSelector"
      via: "import and widget composition in _buildAnalyseTab"
      pattern: "MonthSelector"
    - from: "lib/screens/water_screen.dart"
      to: "MonthlySummaryCard"
      via: "import and widget composition in _buildAnalyseTab"
      pattern: "MonthlySummaryCard"
---

<objective>
Redesign the water analytics Analyse tab to use the unified month-based design with shared widgets from Phase 27, following the exact composition pattern established by the electricity screen in Phase 29.

Purpose: Water analytics screen gets the same modern month-based UX as electricity, replacing the old year-based navigation and summary widgets.
Output: Updated water_screen.dart with new Analyse tab + updated water_screen_test.dart with new assertions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-water-gas-analytics/30-RESEARCH.md
@.planning/phases/29-electricity-analytics/29-02-SUMMARY.md

Key reference files:
@lib/screens/electricity_screen.dart (reference implementation - lines 156-296 for _buildAnalyseTab, lines 39-48 for initState)
@lib/screens/water_screen.dart (file to modify)
@test/screens/water_screen_test.dart (test file to update)
@test/screens/electricity_screen_test.dart (reference test pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign water screen Analyse tab with shared widgets</name>
  <files>lib/screens/water_screen.dart</files>
  <action>
Modify lib/screens/water_screen.dart following the electricity_screen.dart reference implementation exactly, with these water-specific substitutions:

**1. Add missing imports (at top of file):**
- `import '../widgets/charts/month_selector.dart';`
- `import '../widgets/charts/monthly_summary_card.dart';`
- `import '../widgets/charts/household_comparison_chart.dart';`

**2. Update initState to add setSelectedMonth:**
Replace the current initState body:
```dart
@override
void initState() {
  super.initState();
  WidgetsBinding.instance.addPostFrameCallback((_) {
    final provider = context.read<AnalyticsProvider>();
    provider.setSelectedMeterType(MeterType.water);
    provider.setSelectedMonth(DateTime.now());
    provider.setSelectedYear(DateTime.now().year);
  });
}
```
Critical: Without `setSelectedMonth`, `monthlyData` stays null and Analyse tab shows "No data".

**3. Replace _buildAnalyseTab (and remove _buildAnalyseContent if it exists as a separate method):**
Follow electricity_screen.dart lines 156-296 exactly, with these differences:
- Use `MeterType.water` (not electricity)
- Use `colorForMeterType(MeterType.water)` for the color (blue)
- NO SmartPlugAnalyticsProvider -- omit spProvider, spKwh, spPercent entirely
- MonthlySummaryCard: do NOT pass smartPlugKwh or smartPlugPercent (they're optional params, just omit)
- onMonthChanged: only sync AnalyticsProvider (no spProvider.setSelectedMonth)
- Cost toggle icon: `Icons.water_drop` (used in existing cost toggle, keep as-is)
- Cost meter type: `CostMeterType.water` (keep existing)

The complete widget composition order:
1. MonthSelector with onMonthChanged that syncs month and conditionally syncs year on boundary crossing
2. SizedBox(height: 16)
3. MonthlySummaryCard with totalConsumption, previousMonthTotal, unit, month, color, locale
4. SizedBox(height: 24)
5. Text(l10n.monthlyBreakdown) + MonthlyBarChart with periods, primaryColor, unit, highlightMonth, locale, showCosts, periodCosts, costUnit
6. SizedBox(height: 24)
7. [conditional] YearComparisonChart + ChartLegend (only if yearlyData?.previousYearBreakdown is non-null and non-empty)
8. SizedBox(height: 24)
9. [conditional] HouseholdComparisonChart (only if householdComparisonData.length > 1)

Include the previousMonthTotal extraction pattern (search monthlyData.recentMonths for month-1 period).

**4. Remove dead private widgets:**
Delete the `_YearNavigationHeader` class entirely (was around lines 295-338).
Delete the `_YearlySummaryCard` class entirely (was around lines 341-454).
If `_buildAnalyseContent` exists as a separate method from `_buildAnalyseTab`, remove it and consolidate into `_buildAnalyseTab`.

**5. Clean up unused imports:**
If `app_database.dart` import is no longer needed after removing dead code, remove it. Check that all remaining imports are used.

**Important: Do NOT modify the Liste tab or any other part of the screen.** Only the Analyse tab and initState change.
  </action>
  <verify>
Run `flutter analyze lib/screens/water_screen.dart` -- zero issues.
Grep for `_YearNavigationHeader` and `_YearlySummaryCard` in water_screen.dart -- zero matches (dead code removed).
Grep for `MonthSelector` in water_screen.dart -- at least 1 match (new widget used).
Grep for `setSelectedMonth` in water_screen.dart -- at least 1 match (initState updated).
  </verify>
  <done>
Water screen Analyse tab uses MonthSelector, MonthlySummaryCard, MonthlyBarChart (with highlightMonth), conditional YearComparisonChart, and conditional HouseholdComparisonChart. Old _YearNavigationHeader and _YearlySummaryCard are fully removed. initState calls setSelectedMonth in addition to setSelectedMeterType and setSelectedYear.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update water screen tests for month-based Analyse tab</name>
  <files>test/screens/water_screen_test.dart</files>
  <action>
Update test/screens/water_screen_test.dart following the electricity_screen_test.dart reference pattern:

**1. Add tearDown with 300ms delay:**
The new initState fires async operations (setSelectedMonth + setSelectedYear). Add a tearDown block with `await Future.delayed(const Duration(milliseconds: 300))` BEFORE disposing providers and closing the database. This prevents "used after being disposed" errors. Reference: electricity_screen_test.dart tearDown pattern.

**2. Add new Analyse tab test group:**
Add a test group for the Analyse tab with at minimum these tests:

a) **"Analyse tab shows MonthSelector when data exists"**: Insert a water meter + 2 readings (previous month and current month), pump WaterScreen, tap the "Analysis" tab text (or localized equivalent -- check what tab label the water screen uses), pumpAndSettle with delays, then expect `find.byType(MonthSelector)` findsOneWidget and `find.byType(MonthlySummaryCard)` findsOneWidget.

b) **"Analyse tab shows MonthlyBarChart"**: Same setup as above, verify `find.byType(MonthlyBarChart)` findsOneWidget after navigating to Analyse tab.

c) **"Analyse tab shows no data when no readings"**: Pump WaterScreen with no readings, navigate to Analyse tab, expect to find the "noData" text (l10n.noData).

**3. Update existing Analyse tab tests:**
If any existing tests check for year-based navigation widgets (find.text with year number, year navigation chevrons, _YearlySummaryCard), update them to check for the new month-based widgets instead (MonthSelector, MonthlySummaryCard).

**4. Import new widget types for assertions:**
Add these imports:
- `import 'package:valtra/widgets/charts/month_selector.dart';`
- `import 'package:valtra/widgets/charts/monthly_summary_card.dart';`
- `import 'package:valtra/widgets/charts/monthly_bar_chart.dart';`

**5. Test pattern for water readings:**
Water uses `WaterMetersCompanion.insert` for meters and `WaterReadingsCompanion.insert` for readings. Each reading needs a `waterMeterId` (not a direct `householdId`). Check the existing test setup pattern in the file.

Use `tester.runAsync(() async { ... })` wrapper for all new tests (required for real async operations in flutter_test).

Use `await Future.delayed(const Duration(milliseconds: 200-300))` after inserting data and after tapping tabs before making assertions.

After each test that pumps WaterScreen, clean up with `await tester.pumpWidget(Container())` before the test closes.
  </action>
  <verify>
Run `flutter test test/screens/water_screen_test.dart` -- all tests pass.
Run `flutter test` (full suite) -- all tests pass, no regressions.
Run `flutter analyze` -- zero issues.
  </verify>
  <done>
Water screen tests verify MonthSelector, MonthlySummaryCard, and MonthlyBarChart appear on Analyse tab. TearDown uses 300ms delay pattern. All existing tests still pass. Full test suite passes with zero analyzer issues.
  </done>
</task>

</tasks>

<verification>
1. `flutter analyze` returns zero issues
2. `flutter test test/screens/water_screen_test.dart` -- all tests pass
3. `flutter test` -- full suite passes (no regressions)
4. `grep -c "MonthSelector" lib/screens/water_screen.dart` returns >= 1
5. `grep -c "_YearNavigationHeader" lib/screens/water_screen.dart` returns 0
6. `grep -c "_YearlySummaryCard" lib/screens/water_screen.dart` returns 0
</verification>

<success_criteria>
- Water Analyse tab renders MonthSelector, MonthlySummaryCard, MonthlyBarChart, conditional YearComparisonChart, conditional HouseholdComparisonChart
- Water color scheme (blue) used throughout via colorForMeterType(MeterType.water)
- Old dead code (_YearNavigationHeader, _YearlySummaryCard) fully removed
- initState calls setSelectedMonth(DateTime.now()) to populate monthlyData
- No SmartPlugAnalyticsProvider dependency (water-only)
- All tests pass including full suite
</success_criteria>

<output>
After completion, create `.planning/phases/30-water-gas-analytics/30-01-SUMMARY.md`
</output>
