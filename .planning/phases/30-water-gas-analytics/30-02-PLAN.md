---
phase: 30-water-gas-analytics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/screens/gas_screen.dart
  - test/screens/gas_screen_test.dart
  - test/l10n/german_locale_coverage_test.dart
autonomous: true

must_haves:
  truths:
    - "Gas analytics Analyse tab displays MonthSelector for month-based navigation"
    - "Gas analytics Analyse tab displays MonthlySummaryCard with total m3 and % change vs previous month"
    - "Gas analytics Analyse tab displays MonthlyBarChart with highlighted selected month"
    - "Gas analytics Analyse tab conditionally displays YearComparisonChart and HouseholdComparisonChart"
    - "Old year-based _YearNavigationHeader and _YearlySummaryCard are removed (dead code eliminated)"
  artifacts:
    - path: "lib/screens/gas_screen.dart"
      provides: "Month-based Analyse tab using shared widgets"
      contains: "MonthSelector"
    - path: "test/screens/gas_screen_test.dart"
      provides: "Updated tests verifying new Analyse tab widgets"
      contains: "MonthSelector"
  key_links:
    - from: "lib/screens/gas_screen.dart"
      to: "AnalyticsProvider"
      via: "setSelectedMonth in initState and onMonthChanged"
      pattern: "setSelectedMonth"
    - from: "lib/screens/gas_screen.dart"
      to: "MonthSelector"
      via: "import and widget composition in _buildAnalyseTab"
      pattern: "MonthSelector"
    - from: "lib/screens/gas_screen.dart"
      to: "MonthlySummaryCard"
      via: "import and widget composition in _buildAnalyseTab"
      pattern: "MonthlySummaryCard"
---

<objective>
Redesign the gas analytics Analyse tab to use the unified month-based design with shared widgets from Phase 27, following the exact composition pattern established by the electricity screen in Phase 29.

Purpose: Gas analytics screen gets the same modern month-based UX as electricity, replacing the old year-based navigation and summary widgets.
Output: Updated gas_screen.dart with new Analyse tab + updated gas_screen_test.dart + updated german_locale_coverage_test.dart tearDown timing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-water-gas-analytics/30-RESEARCH.md
@.planning/phases/29-electricity-analytics/29-02-SUMMARY.md

Key reference files:
@lib/screens/electricity_screen.dart (reference implementation - lines 156-296 for _buildAnalyseTab, lines 39-48 for initState)
@lib/screens/gas_screen.dart (file to modify)
@test/screens/gas_screen_test.dart (test file to update)
@test/screens/electricity_screen_test.dart (reference test pattern)
@test/l10n/german_locale_coverage_test.dart (german locale tests using GasScreen -- may need tearDown timing fix)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign gas screen Analyse tab with shared widgets</name>
  <files>lib/screens/gas_screen.dart</files>
  <action>
Modify lib/screens/gas_screen.dart following the electricity_screen.dart reference implementation exactly, with these gas-specific substitutions:

**1. Add missing imports (at top of file):**
- `import '../widgets/charts/month_selector.dart';`
- `import '../widgets/charts/monthly_summary_card.dart';`
- `import '../widgets/charts/household_comparison_chart.dart';`

**2. Update initState to add setSelectedMonth:**
Replace the current initState body:
```dart
@override
void initState() {
  super.initState();
  WidgetsBinding.instance.addPostFrameCallback((_) {
    final provider = context.read<AnalyticsProvider>();
    provider.setSelectedMeterType(MeterType.gas);
    provider.setSelectedMonth(DateTime.now());
    provider.setSelectedYear(DateTime.now().year);
  });
}
```
Critical: Without `setSelectedMonth`, `monthlyData` stays null and Analyse tab shows "No data".

**3. Replace _buildAnalyseTab (and remove _buildAnalyseContent if it exists as a separate method):**
Follow electricity_screen.dart lines 156-296 exactly, with these differences:
- Use `MeterType.gas` (not electricity)
- Use `colorForMeterType(MeterType.gas)` for the color (orange)
- NO SmartPlugAnalyticsProvider -- omit spProvider, spKwh, spPercent entirely
- MonthlySummaryCard: do NOT pass smartPlugKwh or smartPlugPercent (they're optional params, just omit)
- onMonthChanged: only sync AnalyticsProvider (no spProvider.setSelectedMonth)
- Cost toggle icon: `Icons.local_fire_department` (used in existing cost toggle, keep as-is)
- Cost meter type: `CostMeterType.gas` (keep existing)

The complete widget composition order (identical to water plan 30-01):
1. MonthSelector with onMonthChanged that syncs month and conditionally syncs year on boundary crossing
2. SizedBox(height: 16)
3. MonthlySummaryCard with totalConsumption, previousMonthTotal, unit, month, color, locale
4. SizedBox(height: 24)
5. Text(l10n.monthlyBreakdown) + MonthlyBarChart with periods, primaryColor, unit, highlightMonth, locale, showCosts, periodCosts, costUnit
6. SizedBox(height: 24)
7. [conditional] YearComparisonChart + ChartLegend (only if yearlyData?.previousYearBreakdown is non-null and non-empty)
8. SizedBox(height: 24)
9. [conditional] HouseholdComparisonChart (only if householdComparisonData.length > 1)

Include the previousMonthTotal extraction pattern (search monthlyData.recentMonths for month-1 period).

**4. Remove dead private widgets:**
Delete the `_YearNavigationHeader` class entirely (was around lines 391-433).
Delete the `_YearlySummaryCard` class entirely (was around lines 436-550).
If `_buildAnalyseContent` exists as a separate method from `_buildAnalyseTab`, remove it and consolidate into `_buildAnalyseTab`.

**5. Clean up unused imports:**
Check that all remaining imports are used after removing dead code.

**Important: Do NOT modify the Liste tab or any other part of the screen.** Only the Analyse tab and initState change.
  </action>
  <verify>
Run `flutter analyze lib/screens/gas_screen.dart` -- zero issues.
Grep for `_YearNavigationHeader` and `_YearlySummaryCard` in gas_screen.dart -- zero matches (dead code removed).
Grep for `MonthSelector` in gas_screen.dart -- at least 1 match (new widget used).
Grep for `setSelectedMonth` in gas_screen.dart -- at least 1 match (initState updated).
  </verify>
  <done>
Gas screen Analyse tab uses MonthSelector, MonthlySummaryCard, MonthlyBarChart (with highlightMonth), conditional YearComparisonChart, and conditional HouseholdComparisonChart. Old _YearNavigationHeader and _YearlySummaryCard are fully removed. initState calls setSelectedMonth in addition to setSelectedMeterType and setSelectedYear.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update gas screen tests and german locale tests</name>
  <files>test/screens/gas_screen_test.dart, test/l10n/german_locale_coverage_test.dart</files>
  <action>
**Part A: Update test/screens/gas_screen_test.dart** following the electricity_screen_test.dart reference pattern:

**1. Add tearDown with 300ms delay:**
The new initState fires async operations (setSelectedMonth + setSelectedYear). Add a tearDown block with `await Future.delayed(const Duration(milliseconds: 300))` BEFORE disposing providers and closing the database. This prevents "used after being disposed" errors.

**2. Add new Analyse tab test group:**
Add a test group for the Analyse tab with at minimum these tests:

a) **"Analyse tab shows MonthSelector when data exists"**: Insert 2 gas readings (previous month and current month), pump GasScreen, tap the "Analysis" tab text (or localized equivalent), pumpAndSettle with delays, then expect `find.byType(MonthSelector)` findsOneWidget and `find.byType(MonthlySummaryCard)` findsOneWidget.

b) **"Analyse tab shows MonthlyBarChart"**: Same setup as above, verify `find.byType(MonthlyBarChart)` findsOneWidget after navigating to Analyse tab.

c) **"Analyse tab shows no data when no readings"**: Pump GasScreen with no readings, navigate to Analyse tab, expect to find the "noData" text (l10n.noData).

**3. Update existing Analyse tab tests:**
If any existing tests check for year-based navigation widgets (find.text with year number, year navigation chevrons, _YearlySummaryCard), update them to check for the new month-based widgets instead (MonthSelector, MonthlySummaryCard).

**4. Import new widget types for assertions:**
Add these imports:
- `import 'package:valtra/widgets/charts/month_selector.dart';`
- `import 'package:valtra/widgets/charts/monthly_summary_card.dart';`
- `import 'package:valtra/widgets/charts/monthly_bar_chart.dart';`

**5. Gas reading pattern:**
Gas readings use `GasReadingsCompanion.insert(householdId: householdId, timestamp: ..., valueCubicMeters: ...)` -- readings go directly on household (no separate meter entity like water).

Use `tester.runAsync(() async { ... })` wrapper for all new tests.
Use `await Future.delayed(const Duration(milliseconds: 200-300))` after inserting data and after tapping tabs.
After each test that pumps GasScreen, clean up with `await tester.pumpWidget(Container())`.

**Part B: Update test/l10n/german_locale_coverage_test.dart** for gas tearDown timing:

The german locale tests have 2 GasScreen tests (lines ~237-392). The new gas initState fires 2 concurrent async loads (setSelectedMonth + setSelectedYear), which may cause "used after disposed" errors in tearDown. 

Increase the tearDown delay in both gas German tests from 200ms to 300ms (matching the electricity pattern). The delay appears just before provider.dispose() / analyticsProvider.dispose() calls. Look for `await Future.delayed(const Duration(milliseconds: 200))` before dispose calls in the gas test blocks and change to 300ms.
  </action>
  <verify>
Run `flutter test test/screens/gas_screen_test.dart` -- all tests pass.
Run `flutter test test/l10n/german_locale_coverage_test.dart` -- all tests pass.
Run `flutter test` (full suite) -- all tests pass, no regressions.
Run `flutter analyze` -- zero issues.
  </verify>
  <done>
Gas screen tests verify MonthSelector, MonthlySummaryCard, and MonthlyBarChart appear on Analyse tab. TearDown uses 300ms delay pattern. German locale gas tests updated with 300ms tearDown delay. All tests pass. Full test suite passes with zero analyzer issues.
  </done>
</task>

</tasks>

<verification>
1. `flutter analyze` returns zero issues
2. `flutter test test/screens/gas_screen_test.dart` -- all tests pass
3. `flutter test test/l10n/german_locale_coverage_test.dart` -- all tests pass
4. `flutter test` -- full suite passes (no regressions)
5. `grep -c "MonthSelector" lib/screens/gas_screen.dart` returns >= 1
6. `grep -c "_YearNavigationHeader" lib/screens/gas_screen.dart` returns 0
7. `grep -c "_YearlySummaryCard" lib/screens/gas_screen.dart` returns 0
</verification>

<success_criteria>
- Gas Analyse tab renders MonthSelector, MonthlySummaryCard, MonthlyBarChart, conditional YearComparisonChart, conditional HouseholdComparisonChart
- Gas color scheme (orange) used throughout via colorForMeterType(MeterType.gas)
- Old dead code (_YearNavigationHeader, _YearlySummaryCard) fully removed
- initState calls setSelectedMonth(DateTime.now()) to populate monthlyData
- No SmartPlugAnalyticsProvider dependency (gas-only)
- German locale gas tests have 300ms tearDown delay
- All tests pass including full suite
</success_criteria>

<output>
After completion, create `.planning/phases/30-water-gas-analytics/30-02-SUMMARY.md`
</output>
