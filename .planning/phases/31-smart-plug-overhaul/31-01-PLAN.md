---
phase: 31-smart-plug-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/services/analytics/analytics_models.dart
  - lib/providers/smart_plug_analytics_provider.dart
  - lib/screens/smart_plug_analytics_screen.dart
  - test/screens/smart_plug_analytics_screen_test.dart
autonomous: true

must_haves:
  truths:
    - "Smart plug Analyse tab displays MonthSelector, MonthlySummaryCard, MonthlyBarChart, YearComparisonChart, and HouseholdComparisonChart using shared widgets"
    - "Per-plug pie chart shows consumption breakdown with single-hue yellow shades (not rainbow)"
    - "Per-plug list breakdown shows each plug's consumption for selected month"
    - "Room-based consumption sections (pie chart and list) are removed from the analytics screen"
    - "MonthlySummaryCard does NOT show smart plug coverage line (would be redundant on smart plugs screen)"
  artifacts:
    - path: "lib/services/analytics/analytics_models.dart"
      provides: "smartPlugPieColors constant (10 yellow shades)"
      contains: "smartPlugPieColors"
    - path: "lib/providers/smart_plug_analytics_provider.dart"
      provides: "byPlug colors using smartPlugPieColors instead of pieChartColors"
      contains: "smartPlugPieColors"
    - path: "lib/screens/smart_plug_analytics_screen.dart"
      provides: "Redesigned SmartPlugAnalyseTab with shared widget composition"
      contains: "MonthSelector"
    - path: "test/screens/smart_plug_analytics_screen_test.dart"
      provides: "Tests for shared widget presence and per-plug breakdown"
  key_links:
    - from: "lib/screens/smart_plug_analytics_screen.dart"
      to: "AnalyticsProvider"
      via: "context.watch<AnalyticsProvider>()"
      pattern: "context\\.watch<AnalyticsProvider>"
    - from: "lib/screens/smart_plug_analytics_screen.dart"
      to: "SmartPlugAnalyticsProvider"
      via: "context.watch<SmartPlugAnalyticsProvider>()"
      pattern: "context\\.watch<SmartPlugAnalyticsProvider>"
    - from: "lib/providers/smart_plug_analytics_provider.dart"
      to: "lib/services/analytics/analytics_models.dart"
      via: "import smartPlugPieColors"
      pattern: "smartPlugPieColors\\[i"
---

<objective>
Redesign the smart plug Analyse tab to use shared chart widgets (MonthSelector, MonthlySummaryCard, MonthlyBarChart, YearComparisonChart, HouseholdComparisonChart) following the electricity screen reference pattern, and add a single-hue yellow color scheme for the per-plug pie chart.

Purpose: Satisfies SPLG-01 (unified analytics design), SPLG-02 (single-hue colors), SPLG-03 (per-plug pie + list), and the analytics side of SPLG-04 (remove room grouping from Analyse tab).
Output: Redesigned SmartPlugAnalyseTab, smartPlugPieColors constant, updated provider, passing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-smart-plug-overhaul/31-RESEARCH.md
@.planning/phases/29-electricity-analytics/29-02-SUMMARY.md

Key reference files (read these before implementing):
@lib/screens/electricity_screen.dart (lines 156-296: _buildAnalyseTab -- the exact pattern to follow)
@lib/screens/smart_plug_analytics_screen.dart (current code to replace)
@lib/providers/smart_plug_analytics_provider.dart (current provider to modify)
@lib/services/analytics/analytics_models.dart (add smartPlugPieColors here)
@test/screens/smart_plug_analytics_screen_test.dart (current tests to update)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add smartPlugPieColors constant and update provider color assignment</name>
  <files>
    lib/services/analytics/analytics_models.dart
    lib/providers/smart_plug_analytics_provider.dart
  </files>
  <action>
1. In `lib/services/analytics/analytics_models.dart`, add a new `smartPlugPieColors` constant after the existing `pieChartColors` list. Use 10 shades of electricity yellow (0xFFFFD93D) with sufficient brightness difference between adjacent entries:

```dart
/// Single-hue (electricity yellow shades) colors for smart plug pie charts.
/// Satisfies SPLG-02: unified single-hue color scheme.
const List<Color> smartPlugPieColors = [
  Color(0xFFFFD93D), // electricity yellow -- brand
  Color(0xFFEFC000), // darker gold
  Color(0xFFFFE57A), // lighter yellow
  Color(0xFFD4A800), // deep amber
  Color(0xFFFFF0B0), // pale yellow
  Color(0xFFB88F00), // dark gold
  Color(0xFFFFEC99), // light amber
  Color(0xFFCC9900), // medium dark
  Color(0xFFFFF5CC), // very light
  Color(0xFFE6B800), // mid amber
];
```

Colors alternate dark/light to ensure adjacent pie slices are visually distinct.

2. In `lib/providers/smart_plug_analytics_provider.dart`, change the `byPlug` color assignment from `pieChartColors` to `smartPlugPieColors`:
   - Line ~106: change `color: pieChartColors[i % pieChartColors.length]` to `color: smartPlugPieColors[i % smartPlugPieColors.length]`
   - The `byRoom` color assignment can stay as `pieChartColors` (byRoom is kept in the data model but no longer rendered).
  </action>
  <verify>
Run `flutter analyze` -- zero errors/warnings on modified files. Grep for `smartPlugPieColors` confirms usage in both files.
  </verify>
  <done>
`smartPlugPieColors` constant exists in analytics_models.dart with 10 yellow shades. SmartPlugAnalyticsProvider assigns `smartPlugPieColors` for byPlug colors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign SmartPlugAnalyseTab with shared widget composition</name>
  <files>
    lib/screens/smart_plug_analytics_screen.dart
    test/screens/smart_plug_analytics_screen_test.dart
  </files>
  <action>
**Rewrite `lib/screens/smart_plug_analytics_screen.dart`** -- replace the entire file content. The new `SmartPlugAnalyseTab` must:

1. Import shared widgets: `MonthSelector`, `MonthlySummaryCard`, `MonthlyBarChart`, `YearComparisonChart`, `HouseholdComparisonChart`, `ConsumptionPieChart`, `ChartLegend`.
2. Import `AnalyticsProvider` and `analytics_models.dart` (for `MeterType`, `colorForMeterType`, `PieSliceData`, `smartPlugPieColors`).
3. Watch both `AnalyticsProvider` and `SmartPlugAnalyticsProvider`.
4. Follow the electricity screen `_buildAnalyseTab()` pattern exactly (lines 156-296 of electricity_screen.dart), with these differences:
   - No `_showCosts` toggle (smart plugs have no cost profiles). Pass `showCosts: false` to MonthlyBarChart and YearComparisonChart.
   - No `smartPlugKwh`/`smartPlugPercent` on MonthlySummaryCard (pass null / omit -- would be redundant since the entire screen is about smart plugs).
   - After HouseholdComparisonChart, add per-plug pie chart section:
     - Section title: `l10n.consumptionByPlugTitle`
     - `ConsumptionPieChart` with slices built from `spProvider.data!.byPlug` (same `_buildPlugSlices` helper pattern as current code, but using `smartPlugPieColors` which the provider now assigns)
     - Per-plug list breakdown items (keep `_PlugBreakdownItem` widget, it already works correctly)
   - The `_PlugBreakdownItem` widget is reused from the current file. Keep it as-is.

5. **Remove entirely** from the file:
   - `_MonthNavigation` widget (replaced by `MonthSelector`)
   - `_SummaryCard` widget (replaced by `MonthlySummaryCard`)
   - `_SummaryRow` widget
   - `_RoomBreakdownItem` widget
   - `_buildRoomSlices()` method
   - All room-related sections from `_buildDataSections`

6. Provider initialization pattern: The `SmartPlugAnalyseTab` is a child widget of `SmartPlugsScreen`. The initState for both `AnalyticsProvider` and `SmartPlugAnalyticsProvider` must be handled by `SmartPlugsScreen` (done in Plan 31-02). For now, `SmartPlugAnalyseTab` just reads from both providers via `context.watch`.

7. The `_buildPlugSlices` helper stays but uses `spProvider.data!.totalSmartPlug` as the total denominator (not `totalSmartPlug + otherConsumption`), since we only want the per-plug breakdown out of the smart plug total. If total is 0, return empty slices.

8. For the MonthSelector `onMonthChanged` callback -- sync both providers:
```dart
onMonthChanged: (month) {
  analyticsProvider.setSelectedMonth(month);
  spProvider.setSelectedMonth(month);
  if (month.year != analyticsProvider.selectedYear) {
    analyticsProvider.setSelectedYear(month.year);
  }
},
```

9. Compute `previousMonthTotal` from `monthlyData.recentMonths` inline (same pattern as electricity screen).

10. Use `colorForMeterType(MeterType.electricity)` as the primary color (smart plugs are sub-dimension of electricity).

**Update `test/screens/smart_plug_analytics_screen_test.dart`**:

1. Add `AnalyticsProvider` mock to the test setup. The SmartPlugAnalyseTab now needs `AnalyticsProvider` in the provider tree.
   - Create `MockAnalyticsProvider` (same pattern as MockSmartPlugAnalyticsProvider).
   - Stub `analyticsProvider.isLoading` -> false
   - Stub `analyticsProvider.monthlyData` -> a `MonthlyAnalyticsData` with sensible test data (totalConsumption: 100, unit: 'kWh', recentMonths with at least the current month entry)
   - Stub `analyticsProvider.yearlyData` -> null (to skip year comparison section for simplicity)
   - Stub `analyticsProvider.selectedMonth` -> `DateTime(2025, 1, 1)`
   - Stub `analyticsProvider.selectedYear` -> 2025
   - Stub `analyticsProvider.householdComparisonData` -> empty list (skip household comparison in tests)
   - Add to `buildSubject()` MultiProvider.

2. Remove all tests checking for `_SummaryCard`, `_MonthNavigation`, room pie chart, room breakdown items, `consumptionByRoomTitle`.

3. Add new tests verifying:
   - `MonthSelector` widget is present
   - `MonthlySummaryCard` widget is present
   - `MonthlyBarChart` widget is present (only when monthlyData has recentMonths)
   - Per-plug pie chart (`ConsumptionPieChart`) is present when byPlug data exists
   - Per-plug breakdown items (check for plug names in text)
   - Empty state shows when both providers have no data
   - Loading state shows `CircularProgressIndicator`

4. Update `createSampleData()` -- remove the `byRoom` entries from test data (or keep them as empty list since they are no longer rendered). The `SmartPlugAnalyticsData` constructor still requires `byRoom`, so pass an empty list.

5. Add 300ms tearDown delay (same pattern as electricity tests) since the Analyse tab now triggers multiple async provider loads.

6. Ensure `flutter test test/screens/smart_plug_analytics_screen_test.dart` passes, then run full `flutter test` to check for regressions. Also run `flutter analyze`.
  </action>
  <verify>
`flutter test test/screens/smart_plug_analytics_screen_test.dart` -- all tests pass.
`flutter test` -- all project tests pass (expect 1 pre-existing migration_test failure).
`flutter analyze` -- zero new issues.
  </verify>
  <done>
SmartPlugAnalyseTab renders MonthSelector, MonthlySummaryCard, MonthlyBarChart, YearComparisonChart (conditional), HouseholdComparisonChart (conditional), and per-plug pie chart + list. Room sections removed. Tests updated and passing. Single-hue colors visible in pie chart.
  </done>
</task>

</tasks>

<verification>
1. `flutter analyze` reports zero errors/warnings
2. `flutter test` passes all tests (1226+ total, minus 1 pre-existing migration failure)
3. `grep -r 'smartPlugPieColors' lib/` shows usage in analytics_models.dart and smart_plug_analytics_provider.dart
4. `grep -r 'consumptionByRoomTitle' lib/screens/smart_plug_analytics_screen.dart` returns nothing (room sections removed)
5. `grep -r 'MonthSelector' lib/screens/smart_plug_analytics_screen.dart` confirms shared widget usage
</verification>

<success_criteria>
- SmartPlugAnalyseTab uses all 5 shared chart widgets from Phase 27
- Per-plug pie chart uses smartPlugPieColors (single-hue yellow), not rainbow pieChartColors
- Room-based consumption sections completely removed from analytics screen
- All existing and new tests pass
- Zero flutter analyze issues
</success_criteria>

<output>
After completion, create `.planning/phases/31-smart-plug-overhaul/31-01-SUMMARY.md`
</output>
