# Plan 15-02: Smart Plug Entry Rework — Data Layer

**Requirement**: FR-13.2 (Smart Plug Entry Rework)
**UAC**: UAC-M3-12
**Wave**: 2a (Data layer — must complete before 15-02b)

## Goal
Update smart plug data model: remove interval type, simplify to month-based consumption entries. Schema changes in `tables.dart` only — migration written in Plan 15-07.

## Current State
- `SmartPlugConsumptions` table: `id`, `smartPlugId`, `intervalType` (enum: daily/weekly/monthly/yearly), `intervalStart` (datetime), `valueKwh` (double)
- `IntervalType` enum used in tables.dart, DAO, provider, form dialog, screen

## Tasks

### T1: Update table definitions (NO migration — Plan 15-07 handles migration)
**Files**: `lib/database/tables.dart`
1. Remove `intervalType` column from `SmartPlugConsumptions` table definition
2. Rename `intervalStart` → `month` (DateTime, represents 1st of month 00:00)
3. Remove `IntervalType` enum from tables.dart
4. Run `dart run build_runner build --delete-conflicting-outputs` to regenerate

### T2: Update SmartPlugDao
**Files**: `lib/database/daos/smart_plug_dao.dart`
1. Update `insertConsumption()` — remove intervalType parameter, use `month` column
2. Update `getConsumptionsForPlug()` — order by `month` descending
3. Update `watchConsumptionsForPlug()` — order by `month` descending
4. Add `getConsumptionForMonth(plugId, month)` — check if entry exists for given month
5. Update aggregation queries (`getTotalConsumptionForPlug`, etc.) to use `month` column
6. Remove any intervalType-related logic

### T3: Update SmartPlugProvider
**Files**: `lib/providers/smart_plug_provider.dart`
1. Update consumption CRUD methods — remove intervalType parameter
2. Update `ConsumptionWithLabel` model — label is now localized month/year string
3. Add duplicate month check before insert
4. Update any method signatures referencing IntervalType

### T4: Update SmartPlugAnalyticsProvider
**Files**: `lib/providers/smart_plug_analytics_provider.dart`
1. Update any queries referencing `intervalStart` → `month`
2. Verify aggregation still works with new column name
3. Ensure pie charts and monthly summaries compute correctly

### T5: Tests
**Files**: `test/database/daos/smart_plug_dao_test.dart`, `test/providers/smart_plug_provider_test.dart`, `test/providers/smart_plug_analytics_provider_test.dart`
1. DAO tests: insert/query without intervalType, month ordering, duplicate detection
2. Provider tests: consumption CRUD without intervalType, label formatting
3. Analytics tests: aggregation with new schema

## Verification
- [ ] No `IntervalType` enum in codebase
- [ ] SmartPlugConsumptions table has `month` column (no `intervalType`, no `intervalStart`)
- [ ] DAO queries work with new column names
- [ ] Provider correctly manages month-based consumption
- [ ] All data-layer tests pass
