---
phase: 23-app-branding-splash
verified: 2026-03-13T14:16:24Z
status: passed
score: 6/6 must-haves verified
---

# Phase 23: App Branding & Splash Verification Report

**Phase Goal:** Set new app icon, capitalize app name on home screen, native splash until data loaded.
**Verified:** 2026-03-13T14:16:24Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

Must-haves were defined in plan frontmatter (both plans). All six truths verified:

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Device home screen shows "Valtra" (capital V) on Android | VERIFIED | `android:label="Valtra"` at AndroidManifest.xml line 3 |
| 2 | Device home screen shows "Valtra" (capital V) on iOS | VERIFIED | `CFBundleName` = `Valtra` and `CFBundleDisplayName` = `Valtra` in Info.plist lines 15-16, 7-8 |
| 3 | App launcher icon is a custom glassmorphism house/gauge icon | VERIFIED | `assets/icon/icon.png` is a valid 1024x1024 RGBA PNG (52,583 bytes; Python generate script confirms dimensions) |
| 4 | App startup shows Ultra Violet splash screen instead of white/empty screen | VERIFIED | `flutter_native_splash` configured with `color: "#5F4A8B"`, `android_12:` block present, assets generated; `values-v31/styles.xml` exists for Android 12+ |
| 5 | Splash screen remains visible until household data is loaded | VERIFIED | `FlutterNativeSplash.preserve(widgetsBinding: widgetsBinding)` called in `main()` before any async work; `removeSplashWhenReady` registered via HouseholdProvider listener after `runApp` |
| 6 | After households load, splash is removed and home screen appears | VERIFIED | `removeSplashWhenReady` waits for first `notifyListeners()` (DB stream event) then calls `FlutterNativeSplash.remove` via `addPostFrameCallback`; 2 tests confirm this lifecycle; all 1079 tests pass |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `assets/icon/icon.png` | 1024x1024 source icon PNG | VERIFIED | Exists, 52,583 bytes, Python script confirms 1024x1024 dimensions |
| `pubspec.yaml` (launcher icons) | `flutter_launcher_icons` dep + config | VERIFIED | Dev dep `^0.14.4` present; `flutter_launcher_icons:` block with `image_path: "assets/icon/icon.png"` at root level |
| `android/app/src/main/res/mipmap-{hdpi,mdpi,xhdpi,xxhdpi,xxxhdpi}/launcher_icon.png` | Generated Android mipmap icons | VERIFIED | All 5 density variants exist |
| `android/app/src/main/AndroidManifest.xml` | `android:label="Valtra"` | VERIFIED | Line 3: `android:label="Valtra"` |
| `ios/Runner/Info.plist` | `CFBundleName = Valtra` | VERIFIED | Line 16: `<string>Valtra</string>` under `CFBundleName`; also `CFBundleDisplayName = Valtra` |
| `pubspec.yaml` (native splash) | `flutter_native_splash` dep + config | VERIFIED | Runtime dep `^2.4.7`; `flutter_native_splash:` block with `color: "#5F4A8B"`, `image: assets/splash/splash_logo.png`, `android_12:` block |
| `assets/splash/splash_logo.png` | Splash screen logo image | VERIFIED | Exists, 52,583 bytes (copy of icon.png) |
| `android/app/src/main/res/values-v31/styles.xml` | Android 12+ splash style | VERIFIED | File exists, generated by flutter_native_splash |
| `lib/main.dart` | Splash preserve/remove lifecycle, 508 lines | VERIFIED | 508 lines; `FlutterNativeSplash.preserve` at line 46; `removeSplashWhenReady` defined lines 159-173 and called at line 148; `@visibleForTesting` annotation present |
| `test/main_splash_test.dart` | Splash removal tests (min 30 lines) | VERIFIED | 77 lines; 2 testWidgets covering normal and empty-DB cases |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `pubspec.yaml` | `assets/icon/icon.png` | `image_path` config | WIRED | `image_path: "assets/icon/icon.png"` present at line 129 |
| `lib/main.dart` | `FlutterNativeSplash.preserve` | Called before async work in `main()` | WIRED | Line 46: `FlutterNativeSplash.preserve(widgetsBinding: widgetsBinding)` — immediately after `ensureInitialized()`, before all `await` calls |
| `lib/main.dart` | `FlutterNativeSplash.remove` | `removeSplashWhenReady` listener on `HouseholdProvider` | WIRED | `remove()` called inside `addPostFrameCallback` in the listener; injected via `removeSplash` parameter for testing |
| `lib/main.dart` | `HouseholdProvider.isInitialized` / stream event | Listener on provider fires after first `notifyListeners()` | WIRED | `provider.addListener(listener)` at line 172; listener fires on first DB stream event (not on `isInitialized` directly, which is correct — it waits for stream confirmation) |
| `test/main_splash_test.dart` | `removeSplashWhenReady` in `lib/main.dart` | Direct call via `@visibleForTesting` public function | WIRED | Imported from `package:valtra/main.dart`; both tests call the function with `removeSplash:` injection |

### Requirements Coverage

| Requirement | Status | Notes |
|-------------|--------|-------|
| BRAND-01: Custom app icon (glassmorphism house/gauge) | SATISFIED | 1024x1024 PNG with Ultra Violet gradient, house silhouette + gauge arc; all mipmap and iOS AppIcon sizes generated |
| BRAND-02: App name capitalized to "Valtra" on both platforms | SATISFIED | AndroidManifest `android:label="Valtra"`; Info.plist `CFBundleName` = `Valtra` and `CFBundleDisplayName` = `Valtra` |
| LOAD-01: Native splash screen persists until household data loaded | SATISFIED | `preserve` before async init; listener on HouseholdProvider waits for first DB stream event before calling `remove` via `addPostFrameCallback` |

### Anti-Patterns Found

None. Scanned `lib/main.dart` and `test/main_splash_test.dart` for TODO/FIXME/placeholder/return null/console.log patterns — none found. `removeSplashWhenReady` body is a real implementation with listener lifecycle, not a stub.

### Human Verification Required

| Test | What to do | Expected | Why human |
|------|------------|----------|-----------|
| App icon appearance | Install debug APK on Android device, long-press home screen to view app icons | Ultra Violet gradient with white house silhouette and gauge arc visible | Visual appearance cannot be verified programmatically |
| iOS app icon | Build and install on iOS simulator or device | Same icon design displayed in SpringBoard | Requires iOS build |
| Home screen label | After install, view launcher icon label on Android home screen | "Valtra" (capital V) displayed below icon | Native label rendering requires device |
| Splash color on first launch | Cold-launch app on device | Ultra Violet (#5F4A8B) background shown from first frame, no white flash | Native splash rendering requires device; Android 12+ behavior especially |
| Splash duration | Cold-launch app with empty DB | Splash stays visible for the duration of DB init, disappears cleanly when home screen is ready | Real async timing cannot be verified in unit tests |

## Gaps Summary

No gaps. All six observable truths are supported by concrete, substantive, wired artifacts:

- Icon pipeline: source PNG exists and is the right size; `flutter_launcher_icons` configured and run; all Android mipmap densities and iOS AppIcon sizes confirmed present.
- App name: both `AndroidManifest.xml` and `Info.plist` contain the capitalized "Valtra" string exactly where the platforms read it.
- Splash lifecycle: the three-part wiring (preserve → runApp → removeSplashWhenReady listener) is fully implemented in `lib/main.dart` with no stubs. The listener correctly waits for the DB stream event rather than the already-true `isInitialized` flag. Two unit tests confirm both the normal case and the empty-DB edge case, and all 1079 project tests pass with zero analyzer issues.

---

_Verified: 2026-03-13T14:16:24Z_
_Verifier: Claude (gsd-verifier)_
