---
phase: 14-ui-ux-polish
plan: 05
type: execute
wave: 3
depends_on: ["14-01", "14-03"]
files_modified:
  - lib/screens/electricity_screen.dart
  - lib/screens/gas_screen.dart
  - lib/screens/water_screen.dart
  - lib/screens/smart_plugs_screen.dart
  - lib/screens/smart_plug_consumption_screen.dart
  - lib/screens/smart_plug_analytics_screen.dart
  - lib/screens/settings_screen.dart
  - lib/widgets/dialogs/electricity_reading_form_dialog.dart
  - lib/widgets/dialogs/gas_reading_form_dialog.dart
  - lib/widgets/dialogs/water_reading_form_dialog.dart
  - lib/widgets/dialogs/heating_reading_form_dialog.dart
  - lib/widgets/dialogs/smart_plug_consumption_form_dialog.dart
  - lib/widgets/dialogs/water_meter_form_dialog.dart
  - lib/widgets/dialogs/smart_plug_form_dialog.dart
autonomous: true
requirements:
  - FR-12.3.1
  - FR-12.3.2
  - FR-12.3.3
  - FR-12.3.4
  - FR-12.3.5
  - FR-12.3.6
  - FR-12.3.7
  - FR-12.4.3
  - FR-12.5.1
  - FR-12.5.2

must_haves:
  truths:
    - "No unit badge chips (kWh, m3) appear in any app bar"
    - "No non-clickable info icons appear anywhere in the app"
    - "No too-long hints in meter reading input fields"
    - "Date/time picker fields are styled as outlined input fields (InputDecorator)"
    - "Smart plug form does not pre-select a room when adding new"
    - "Smart plug form has no hint text in name field"
    - "Settings screen has no interpolation method setting"
    - "Water screen uses filled icons everywhere (cold blue, hot red, other gray)"
    - "Water type selector is a DropdownButtonFormField (not SegmentedButton)"
    - "Smart plug consumption room subtitle is readable in light theme"
  artifacts:
    - path: "lib/widgets/dialogs/water_meter_form_dialog.dart"
      provides: "Water type dropdown instead of segmented button"
      contains: "DropdownButtonFormField"
    - path: "lib/widgets/dialogs/smart_plug_form_dialog.dart"
      provides: "No pre-selected room, no name hint"
    - path: "lib/widgets/dialogs/electricity_reading_form_dialog.dart"
      provides: "InputDecorator-styled date/time picker"
      contains: "InputDecorator"
  key_links:
    - from: "lib/widgets/dialogs/*_form_dialog.dart"
      to: "InputDecorator"
      via: "InkWell + InputDecorator pattern for date/time fields"
      pattern: "InputDecorator"
---

<objective>
Clean up UI elements across screens and dialogs: remove unit badge chips from app bars, remove non-clickable info icons, remove too-long hints, style date/time pickers as outlined fields, fix smart plug form defaults, remove interpolation settings, fix water screen icons and type selector, fix smart plug room subtitle readability.

Purpose: FR-12.3 (UI element cleanup), FR-12.4.3 (smart plug readability), FR-12.5 (water screen fixes) comprise ~15 small targeted changes across 14 files. Grouping them ensures consistent cleanup in one pass.

Output: 14 modified files with cleaner, more consistent UI.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-ui-ux-polish/14-RESEARCH.md

@lib/screens/settings_screen.dart
@lib/widgets/dialogs/smart_plug_form_dialog.dart
@lib/widgets/dialogs/water_meter_form_dialog.dart
@lib/widgets/dialogs/electricity_reading_form_dialog.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unit badges, info icons, hints, and fix settings/smart plug/water screens</name>
  <files>
    lib/screens/electricity_screen.dart,
    lib/screens/gas_screen.dart,
    lib/screens/water_screen.dart,
    lib/screens/smart_plugs_screen.dart,
    lib/screens/smart_plug_consumption_screen.dart,
    lib/screens/smart_plug_analytics_screen.dart,
    lib/screens/settings_screen.dart,
    lib/widgets/dialogs/water_meter_form_dialog.dart,
    lib/widgets/dialogs/smart_plug_form_dialog.dart
  </files>
  <action>
    **FR-12.3.1 — Remove unit badge chips from app bar headers (4 files):**
    - electricity_screen.dart: Remove `Chip(label: Text(l10n.kWh))` from AppBar actions
    - gas_screen.dart: Remove `Chip(label: Text(l10n.cubicMeters))` from AppBar actions
    - water_screen.dart: Remove `Chip(label: Text(l10n.cubicMeters))` from AppBar actions
    - smart_plugs_screen.dart: Remove `Chip(label: Text(l10n.kWh))` from AppBar actions

    **FR-12.3.2 — Remove non-clickable info icons (2 files):**
    - settings_screen.dart: Remove `leading: Icon(Icons.info_outline)` from the About section ListTile
    - smart_plug_analytics_screen.dart: Remove `Icons.info_outline` and its tooltip/text explaining "Sonstiger" kWh

    **FR-12.3.6 — No hint text in smart plug name field (1 file):**
    - smart_plug_form_dialog.dart: Remove `hintText: l10n.smartPlugNameHint` from the name TextFormField

    **FR-12.3.5 — No pre-selected room when adding new smart plug (1 file):**
    - smart_plug_form_dialog.dart: Change initial room selection logic:
      - Current: `_selectedRoomId = widget.plug?.roomId ?? widget.initialRoomId ?? widget.rooms.firstOrNull?.id;`
      - New: `_selectedRoomId = widget.plug?.roomId ?? widget.initialRoomId;`
      - This means new plugs (widget.plug == null, widget.initialRoomId == null) will have no pre-selected room

    **FR-12.3.7 — Remove interpolation method setting from settings (1 file):**
    - settings_screen.dart: Remove the `_meterTypes` constant and `_buildInterpolationRow` method
    - Remove the interpolation method label and dropdown rows from the meter settings section
    - Keep the gas conversion factor field

    **FR-12.4.3 — Smart plug room subtitle readability (1 file):**
    - smart_plug_consumption_screen.dart: Change room subtitle style from `colorScheme.onSurfaceVariant` to use `Theme.of(context).textTheme.bodyMedium` with increased font weight (FontWeight.w500) for better readability in both themes

    **FR-12.5.1 — Water screen filled icons (1 file):**
    - water_screen.dart: Replace `Icons.water_drop_outlined` with `Icons.water_drop` (filled) for cold water
    - Replace `Icons.category_outlined` with `Icons.water_drop` (filled, gray color) for "other" water type
    - Replace `Icons.water_drop_outlined` with `Icons.water_drop` in empty state icon

    **FR-12.5.2 — Water type selector: SegmentedButton -> Dropdown (1 file):**
    - water_meter_form_dialog.dart: Replace `SegmentedButton<WaterMeterType>` with `DropdownButtonFormField<WaterMeterType>`
    - Keep the same state variable `_selectedType`
    - Map WaterMeterType values to DropdownMenuItem with filled water_drop icons:
      - cold: blue icon + localized label
      - hot: red icon + localized label
      - other: gray icon + localized label
    - Use `onChanged: (value) => setState(() => _selectedType = value!)`
  </action>
  <verify>
    <automated>flutter analyze --no-pub && flutter test --no-pub</automated>
  </verify>
  <done>
    No unit badge chips in any app bar. No non-clickable info icons. No hint in smart plug name field.
    No pre-selected room for new smart plugs. No interpolation method in settings.
    Water uses filled icons and DropdownButtonFormField for type. Smart plug room subtitle readable.
    All tests pass, flutter analyze clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Style date/time picker fields as outlined InputDecorator in all form dialogs</name>
  <files>
    lib/widgets/dialogs/electricity_reading_form_dialog.dart,
    lib/widgets/dialogs/gas_reading_form_dialog.dart,
    lib/widgets/dialogs/water_reading_form_dialog.dart,
    lib/widgets/dialogs/heating_reading_form_dialog.dart,
    lib/widgets/dialogs/smart_plug_consumption_form_dialog.dart
  </files>
  <action>
    For each of the 5 reading form dialog files, replace the ListTile-based date/time picker with an InputDecorator-based styled field:

    **Current pattern (ListTile):**
    ```dart
    ListTile(
      contentPadding: EdgeInsets.zero,
      leading: const Icon(Icons.calendar_today),
      title: Text(l10n.dateAndTime),
      subtitle: Text(_formatDateTime(_selectedDateTime)),
      onTap: _selectDateTime,
    ),
    ```

    **New pattern (InputDecorator):**
    ```dart
    InkWell(
      onTap: _selectDateTime,
      child: InputDecorator(
        decoration: InputDecoration(
          labelText: l10n.dateAndTime,
          suffixIcon: const Icon(Icons.calendar_today),
        ),
        child: Text(_formatDateTime(_selectedDateTime)),
      ),
    ),
    ```

    This makes the date/time picker visually match the meter reading value input field (both have outlined borders from the InputDecorationTheme).

    **FR-12.3.3 — Remove too-long hints (while touching these files):**
    - In each reading form dialog, check for `hintText: l10n.meterValueHint` or similar long hint text in the meter value TextFormField
    - Remove the hintText property (the labelText already describes the field)

    **Apply to all 5 files listed.** Each has a similar date/time picker pattern. The smart_plug_consumption_form_dialog may only have a date picker (no time), so adjust accordingly.
  </action>
  <verify>
    <automated>flutter analyze --no-pub && flutter test --no-pub</automated>
  </verify>
  <done>
    All 5 reading form dialogs use InputDecorator for date/time picker fields.
    No too-long hints remain in meter value input fields.
    Date/time fields visually match the outlined style of value input fields.
    All tests pass, flutter analyze clean.
  </done>
</task>

</tasks>

<verification>
- `flutter test --no-pub` — all tests pass
- `flutter analyze --no-pub` — zero issues
- Grep for `Chip(label:` in lib/screens/ returns zero (no unit badge chips)
- Grep for `info_outline` in lib/screens/ returns zero (no info icons)
- Grep for `SegmentedButton` in water_meter_form_dialog.dart returns zero (replaced with dropdown)
</verification>

<success_criteria>
- All FR-12.3 requirements implemented (badges, icons, hints, pickers, smart plug, interpolation)
- FR-12.4.3 implemented (room subtitle readable)
- FR-12.5.1 and FR-12.5.2 implemented (water filled icons, dropdown)
- All tests pass, flutter analyze clean
</success_criteria>

<output>
After completion, create `.planning/phases/14-ui-ux-polish/14-05-SUMMARY.md`
</output>
