---
phase: 32-heating-analytics-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/screens/heating_screen.dart
  - test/screens/heating_screen_test.dart
autonomous: true

must_haves:
  truths:
    - "Heating Analyse tab displays MonthSelector, MonthlySummaryCard, MonthlyBarChart, YearComparisonChart, and HouseholdComparisonChart using shared widgets"
    - "MonthSelector syncs AnalyticsProvider month and year (year boundary detection)"
    - "MonthlySummaryCard shows total consumption in 'units' with % change vs previous month"
    - "Per-heater pie chart shows percentage distribution of raw counter reading deltas across heaters for the selected month"
    - "Per-heater list below pie shows each meter's name, room, and percentage"
    - "No cost toggle exists on heating Analyse tab (heating has no cost config)"
    - "Deprecated _YearNavigationHeader and _YearlySummaryCard private classes are removed"
  artifacts:
    - path: "lib/screens/heating_screen.dart"
      provides: "Heating Analyse tab with shared widget composition + per-heater pie chart"
      contains: "MonthSelector"
    - path: "test/screens/heating_screen_test.dart"
      provides: "Tests for heating Analyse tab with shared widgets and pie chart"
      contains: "MonthSelector"
  key_links:
    - from: "lib/screens/heating_screen.dart"
      to: "AnalyticsProvider"
      via: "initState calls setSelectedMeterType + setSelectedMonth + setSelectedYear"
      pattern: "setSelectedMonth"
    - from: "lib/screens/heating_screen.dart"
      to: "HeatingProvider"
      via: "context.watch for metersWithRooms + getReadingsWithDeltas"
      pattern: "getReadingsWithDeltas"
    - from: "lib/screens/heating_screen.dart"
      to: "ConsumptionPieChart"
      via: "_buildHeaterSlices computing per-meter monthly deltas"
      pattern: "ConsumptionPieChart"
---

<objective>
Overhaul the heating Analyse tab to use the shared widget composition pattern (HEAT-01) and add a per-heater pie chart with percentage distribution (HEAT-02).

Purpose: The heating screen is the last remaining screen using the old yearly-only Analyse tab pattern. This plan brings it in line with electricity, gas, water, and smart plug screens, and adds unique per-heater distribution visualization.

Output: Updated heating_screen.dart with month-based Analyse tab + per-heater pie chart, comprehensive tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-heating-analytics-cleanup/32-RESEARCH.md

# Reference implementations
@lib/screens/gas_screen.dart (direct reference for Analyse tab composition)
@lib/screens/smart_plug_analytics_screen.dart (reference for pie chart + breakdown list pattern)
@lib/screens/heating_screen.dart (current file to modify)
@lib/providers/heating_provider.dart (HeatingProvider API: metersWithRooms, getReadingsWithDeltas)
@lib/widgets/charts/consumption_pie_chart.dart (ConsumptionPieChart widget API)
@lib/services/analytics/analytics_models.dart (PieSliceData, pieChartColors, colorForMeterType, unitForMeterType, MeterType)
@test/screens/heating_screen_test.dart (current test file)
@test/screens/gas_screen_test.dart (reference test pattern with 300ms tearDown)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Overhaul heating Analyse tab + per-heater pie chart</name>
  <files>lib/screens/heating_screen.dart, test/screens/heating_screen_test.dart</files>
  <action>
**In lib/screens/heating_screen.dart:**

1. **Fix initState** to call all three required provider setters (the current initState is BROKEN -- missing setSelectedMonth):
   ```dart
   @override
   void initState() {
     super.initState();
     WidgetsBinding.instance.addPostFrameCallback((_) {
       final provider = context.read<AnalyticsProvider>();
       provider.setSelectedMeterType(MeterType.heating);
       provider.setSelectedMonth(DateTime.now());
       provider.setSelectedYear(DateTime.now().year);
     });
   }
   ```

2. **Remove the cost toggle** -- heating has NO cost config, so remove `_showCosts` state and any cost toggle icon from appBar actions. There is currently no cost toggle in the heating screen, so just confirm it stays absent.

3. **Replace `_buildAnalyseTab` and `_buildAnalyseContent`** with the gas_screen.dart composition pattern. The method should:
   - Watch both `AnalyticsProvider` and `HeatingProvider` (HeatingProvider needed for pie chart)
   - Watch `LocaleProvider` for locale string
   - Get `color` from `colorForMeterType(MeterType.heating)`
   - Get `monthlyData` and `yearlyData` from AnalyticsProvider
   - Show loading spinner if `analyticsProvider.isLoading`
   - Show `l10n.noData` if `monthlyData == null`
   - Compute `previousMonthTotal` from `monthlyData.recentMonths` (same pattern as gas_screen.dart lines 170-180)
   - Build `ListView` with this exact composition order:
     a. `MonthSelector` with `onMonthChanged` that calls `analyticsProvider.setSelectedMonth(month)` and syncs year if boundary crossed (`if (month.year != analyticsProvider.selectedYear) analyticsProvider.setSelectedYear(month.year)`)
     b. `MonthlySummaryCard` with `totalConsumption`, `previousMonthTotal`, `unit: monthlyData.unit`, `month`, `color`, `locale` -- NO smartPlugKwh/smartPlugPercent
     c. Monthly breakdown section title + `MonthlyBarChart` in SizedBox(height: 200) with `highlightMonth: analyticsProvider.selectedMonth`, `showCosts: false`, `periodCosts: null`, `costUnit: null`
     d. Year comparison section (conditional: `yearlyData != null && yearlyData.previousYearBreakdown != null && isNotEmpty`) with `YearComparisonChart` in SizedBox(height: 250), `showCosts: false`, all cost params null + `ChartLegend`
     e. Household comparison section (conditional: `analyticsProvider.householdComparisonData.length > 1`) with `HouseholdComparisonChart` in SizedBox(height: 250)
     f. Per-heater pie chart section (HEAT-02) -- see below

4. **Add per-heater pie chart section (HEAT-02):**
   - Create a private method `_buildHeaterSlices(HeatingProvider heatingProvider, DateTime selectedMonth)` that returns `List<PieSliceData>`:
     - Iterate `heatingProvider.metersWithRooms`
     - For each meter, call `heatingProvider.getReadingsWithDeltas(mwr.meter.id)`
     - Sum all deltas where `r.reading.timestamp` falls within the selected month (>= monthStart and < monthEnd where monthStart = `DateTime(selectedMonth.year, selectedMonth.month, 1)` and monthEnd = `DateTime(selectedMonth.year, selectedMonth.month + 1, 1)`)
     - Use RAW reading deltas (NOT ratio-adjusted) per requirement "unitless counter readings"
     - Compute percentage as `(meterDelta / totalDelta) * 100`
     - Use `pieChartColors` (multi-hue), NOT `smartPlugPieColors` (single-hue yellow is for smart plugs only)
     - Return empty list if no deltas found
   - In the ListView, after household comparison, add:
     - Section title (use l10n key for heating distribution or a descriptive title like "Per-Heater Distribution" -- check if l10n key `consumptionByHeaterTitle` or similar exists; if not, use `l10n.monthlyBreakdown` or add a simple title Text)
     - `ConsumptionPieChart(slices: slices, unit: 'units', locale: locale)` in SizedBox(height: 250) -- only show if slices.isNotEmpty
     - `_HeaterBreakdownItem` list below the pie chart

5. **Create `_HeaterBreakdownItem` private widget** following the `_PlugBreakdownItem` pattern from smart_plug_analytics_screen.dart:
   - Props: `meterName`, `roomName`, `percentage`, `value` (raw delta), `color`, `locale`
   - ListTile with dense/compact layout, colored circle leading, meter name title, room name subtitle, trailing shows percentage with `toStringAsFixed(1)%`

6. **Delete deprecated private classes:**
   - Remove the entire `_YearNavigationHeader` class (lines 318-362)
   - Remove the entire `_YearlySummaryCard` class (lines 365-478)
   - Remove the old `_buildAnalyseContent` method

7. **Update imports** -- add these imports that are currently missing:
   - `../widgets/charts/household_comparison_chart.dart`
   - `../widgets/charts/month_selector.dart`
   - `../widgets/charts/monthly_summary_card.dart`
   - `../widgets/charts/consumption_pie_chart.dart`
   - Remove any imports that become unused after removing old classes (e.g., `chart_legend.dart` may still be needed for year comparison legend)

**In test/screens/heating_screen_test.dart:**

1. **Fix tearDown** to add 300ms delay before disposal (matching gas/water pattern):
   ```dart
   tearDown(() async {
     await Future.delayed(const Duration(milliseconds: 300));
     provider.dispose();
     analyticsProvider.dispose();
     costConfigProvider.dispose();
     await database.close();
   });
   ```

2. **Add Analyse tab tests** following gas_screen_test.dart patterns:
   - Test: Analyse tab shows MonthSelector widget
   - Test: Analyse tab shows MonthlySummaryCard widget
   - Test: Analyse tab shows MonthlyBarChart widget
   - Test: Analyse tab shows loading indicator when analytics loading
   - Test: Analyse tab shows noData text when monthlyData is null
   - Test: MonthSelector navigation triggers provider update

3. **Add per-heater pie chart tests:**
   - Test: pie chart renders when heating meters have readings with deltas in selected month
   - Test: pie chart not shown when no heating meter readings exist
   - Test: breakdown list items display meter names and percentages

4. **Update existing tests** that may reference the old Analyse tab widgets (_YearNavigationHeader, _YearlySummaryCard) -- remove or replace those assertions. The Liste tab tests should remain unchanged since the Liste tab is not modified.

5. **Add import for new shared widgets** referenced in assertions:
   ```dart
   import 'package:valtra/widgets/charts/month_selector.dart';
   import 'package:valtra/widgets/charts/monthly_bar_chart.dart';
   import 'package:valtra/widgets/charts/monthly_summary_card.dart';
   ```

**Important anti-patterns to avoid:**
- Do NOT use `SmartPlugAnalyticsProvider` -- heating uses AnalyticsProvider only
- Do NOT use `smartPlugPieColors` -- use `pieChartColors` (multi-hue)
- Do NOT add a cost toggle -- heating has no cost profiles
- Do NOT ratio-adjust the pie chart deltas -- use raw reading deltas per HEAT-02 requirement
- `showCosts: false` always on all chart widgets
  </action>
  <verify>
1. Run `flutter test test/screens/heating_screen_test.dart` -- all tests pass
2. Run `flutter test` -- all project tests pass (expect 1229+ tests)
3. Run `flutter analyze` -- zero errors, zero warnings (info-level deprecation warnings are acceptable)
4. Verify heating_screen.dart no longer contains `_YearNavigationHeader` or `_YearlySummaryCard`
5. Verify heating_screen.dart contains `MonthSelector`, `MonthlySummaryCard`, `MonthlyBarChart`, `YearComparisonChart`, `HouseholdComparisonChart`, `ConsumptionPieChart`
6. Verify initState calls all three: `setSelectedMeterType`, `setSelectedMonth`, `setSelectedYear`
  </verify>
  <done>
- Heating Analyse tab displays full shared widget composition: MonthSelector, MonthlySummaryCard, MonthlyBarChart, YearComparisonChart, HouseholdComparisonChart
- Per-heater pie chart shows percentage distribution of raw counter deltas using pieChartColors
- Per-heater breakdown list shows meter name, room, and percentage for each meter
- Deprecated _YearNavigationHeader and _YearlySummaryCard are removed
- initState correctly calls all three provider setters
- tearDown uses 300ms delay to prevent async disposal races
- All tests pass including new Analyse tab and pie chart tests
  </done>
</task>

</tasks>

<verification>
1. `flutter test test/screens/heating_screen_test.dart` -- all tests pass
2. `flutter test` -- full suite passes (1229+ tests)
3. `flutter analyze` -- clean
4. `grep -c "MonthSelector\|MonthlySummaryCard\|MonthlyBarChart\|YearComparisonChart\|HouseholdComparisonChart\|ConsumptionPieChart" lib/screens/heating_screen.dart` -- all 6 shared widgets present
5. `grep -c "_YearNavigationHeader\|_YearlySummaryCard" lib/screens/heating_screen.dart` -- returns 0 (deprecated classes removed)
</verification>

<success_criteria>
- Heating Analyse tab matches the composition pattern used by electricity, gas, water, and smart plug screens
- Per-heater pie chart renders percentage distribution of unitless counter readings
- All shared chart widgets are used (MonthSelector, MonthlySummaryCard, MonthlyBarChart, YearComparisonChart, HouseholdComparisonChart, ConsumptionPieChart)
- No deprecated private classes remain
- All tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/32-heating-analytics-cleanup/32-01-SUMMARY.md`
</output>
