---
phase: 32-heating-analytics-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/widgets/liquid_glass_widgets.dart
  - lib/screens/households_screen.dart
  - lib/screens/rooms_screen.dart
  - lib/screens/smart_plug_consumption_screen.dart
  - test/screens/smart_plug_consumption_screen_test.dart
  - test/widgets/liquid_glass_widgets_test.dart
  - test/widgets/liquid_glass_widgets_coverage_test.dart
autonomous: true

must_haves:
  truths:
    - "GlassBottomNav class no longer exists in liquid_glass_widgets.dart"
    - "buildGlassFAB function no longer exists in liquid_glass_widgets.dart"
    - "No remaining references to GlassBottomNav or buildGlassFAB in any lib/ or test/ file"
    - "HouseholdsScreen and RoomsScreen use standard FloatingActionButton instead of buildGlassFAB"
    - "SmartPlugConsumptionScreen file and its test file are deleted (confirmed dead code)"
    - "All existing tests still pass after removal"
  artifacts:
    - path: "lib/widgets/liquid_glass_widgets.dart"
      provides: "LiquidGlass widgets without deprecated GlassBottomNav and buildGlassFAB"
    - path: "lib/screens/households_screen.dart"
      provides: "HouseholdsScreen with standard FloatingActionButton"
      contains: "FloatingActionButton"
    - path: "lib/screens/rooms_screen.dart"
      provides: "RoomsScreen with standard FloatingActionButton"
      contains: "FloatingActionButton"
  key_links:
    - from: "lib/screens/households_screen.dart"
      to: "FloatingActionButton"
      via: "Scaffold.floatingActionButton property"
      pattern: "FloatingActionButton"
    - from: "lib/screens/rooms_screen.dart"
      to: "FloatingActionButton"
      via: "Scaffold.floatingActionButton property"
      pattern: "FloatingActionButton"
---

<objective>
Remove deprecated GlassBottomNav and buildGlassFAB from liquid_glass_widgets.dart, migrate their callers, and delete the dead SmartPlugConsumptionScreen (DEBT-01).

Purpose: Eliminate deprecated widget code that has been replaced by LiquidGlassBottomNav since v0.5.0. Clean up dead code that was scheduled for removal in v0.6.0.

Output: Clean liquid_glass_widgets.dart without deprecated symbols, migrated FAB callers, deleted dead screen.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-heating-analytics-cleanup/32-RESEARCH.md

# Files to modify
@lib/widgets/liquid_glass_widgets.dart (remove deprecated GlassBottomNav class + buildGlassFAB function)
@lib/screens/households_screen.dart (replace buildGlassFAB with FloatingActionButton)
@lib/screens/rooms_screen.dart (replace buildGlassFAB with FloatingActionButton)
@lib/screens/smart_plug_consumption_screen.dart (delete entire file -- dead code)
@test/screens/smart_plug_consumption_screen_test.dart (delete entire file -- dead code test)
@test/widgets/liquid_glass_widgets_test.dart (remove deprecated test groups)
@test/widgets/liquid_glass_widgets_coverage_test.dart (remove deprecated test groups)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deprecated widgets and dead code</name>
  <files>lib/widgets/liquid_glass_widgets.dart, lib/screens/households_screen.dart, lib/screens/rooms_screen.dart, test/widgets/liquid_glass_widgets_test.dart, test/widgets/liquid_glass_widgets_coverage_test.dart</files>
  <action>
**In lib/widgets/liquid_glass_widgets.dart:**

1. Delete the entire `GlassBottomNav` class and its `@Deprecated` annotation (currently lines 15-63). This class has ZERO production callers -- all screens already use `LiquidGlassBottomNav`.

2. Delete the entire `buildGlassFAB` function and its `@Deprecated` annotation (currently lines 101-135). This has 3 callers that will be migrated.

3. Keep all other widgets intact: `buildCircleButton`, `buildGlassAppBar`, `liquidGlassSettings`, `buildLiquidCircleButton`, `LiquidGlassBottomNav`, `GlassCard`.

4. Remove the `provider/provider.dart` and `providers/theme_provider.dart` imports ONLY if they become unused after GlassBottomNav removal. Check: `GlassCard` still uses `context.watch<ThemeProvider>()` so `ThemeProvider` import stays. `provider.dart` is still needed for `context.watch`. The `_isDarkFromTheme` helper is used by `buildCircleButton` and `buildGlassAppBar` so it stays.

**In lib/screens/households_screen.dart:**

Replace the `buildGlassFAB` call on line 49 with a standard `FloatingActionButton`:

```dart
// BEFORE:
floatingActionButton: buildGlassFAB(
  context: context,
  icon: Icons.add,
  onPressed: () => _showCreateDialog(context),
  tooltip: l10n.createHousehold,
),

// AFTER:
floatingActionButton: FloatingActionButton(
  onPressed: () => _showCreateDialog(context),
  tooltip: l10n.createHousehold,
  child: const Icon(Icons.add),
),
```

**In lib/screens/rooms_screen.dart:**

Replace the `buildGlassFAB` call on line 29 with a standard `FloatingActionButton`:

```dart
// BEFORE:
floatingActionButton: buildGlassFAB(
  context: context,
  icon: Icons.add,
  onPressed: () => _addRoom(context),
),

// AFTER:
floatingActionButton: FloatingActionButton(
  onPressed: () => _addRoom(context),
  child: const Icon(Icons.add),
),
```

**In test/widgets/liquid_glass_widgets_test.dart:**

Remove the test `group('GlassBottomNav', ...)` block and the test `group('buildGlassFAB', ...)` block entirely. These test groups reference the deleted symbols and will cause compilation errors. Keep all other test groups (buildCircleButton, buildGlassAppBar, GlassCard, LiquidGlassBottomNav, etc.) intact. Also remove any import of ThemeProvider or other symbols that become unused only if they are truly no longer referenced in the remaining tests.

**In test/widgets/liquid_glass_widgets_coverage_test.dart:**

Same as above -- remove test `group('GlassBottomNav', ...)` and `group('buildGlassFAB', ...)` blocks. Keep all other test groups intact.

**Important:** After removing the test groups, check that any `setUp`/`tearDown` in the remaining test structure still works. The groups being removed should be self-contained and their removal should not break the test file structure.
  </action>
  <verify>
1. Run `flutter analyze` -- zero errors (the deprecated warnings for GlassBottomNav and buildGlassFAB should be gone)
2. Run `flutter test test/widgets/liquid_glass_widgets_test.dart` -- passes
3. Run `flutter test test/widgets/liquid_glass_widgets_coverage_test.dart` -- passes
4. Run `flutter test test/screens/households_screen_test.dart` -- passes (FAB replacement works)
5. Run `flutter test test/screens/rooms_screen_test.dart` -- passes
6. Verify no remaining references: `grep -r "buildGlassFAB\|GlassBottomNav" lib/ test/` returns nothing
  </verify>
  <done>
- GlassBottomNav class and buildGlassFAB function removed from liquid_glass_widgets.dart
- HouseholdsScreen uses standard FloatingActionButton
- RoomsScreen uses standard FloatingActionButton
- Test groups for deprecated widgets removed from both test files
- Zero compilation errors, all tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete dead SmartPlugConsumptionScreen</name>
  <files>lib/screens/smart_plug_consumption_screen.dart, test/screens/smart_plug_consumption_screen_test.dart</files>
  <action>
1. **Confirm no navigation references** exist to `SmartPlugConsumptionScreen` anywhere in `lib/` (research already confirmed this -- no `Navigator.push` or route references exist, the screen is dead code after Phase 31 replaced it with expandable cards).

2. **Delete `lib/screens/smart_plug_consumption_screen.dart`** entirely. This file used `buildGlassFAB` (now removed) and is no longer navigated to from anywhere.

3. **Delete `test/screens/smart_plug_consumption_screen_test.dart`** entirely. This test file imports the deleted screen and will fail to compile if kept.

4. **Search for any other references** to `SmartPlugConsumptionScreen` or `smart_plug_consumption_screen` in the codebase (imports, route tables, etc.) and remove them. Based on research, there should be none in `lib/` besides the file itself.

**Why this is safe:** Phase 31 (31-02-PLAN.md) replaced the smart plug Liste tab with expandable cards and removed all navigation to this screen. The accumulated decision notes explicitly state: "SmartPlugConsumptionScreen still exists but is no longer navigated to from smart_plugs_screen.dart; remove in Phase 32 if confirmed unused."
  </action>
  <verify>
1. `ls lib/screens/smart_plug_consumption_screen.dart 2>/dev/null` -- file does not exist
2. `ls test/screens/smart_plug_consumption_screen_test.dart 2>/dev/null` -- file does not exist
3. `grep -r "SmartPlugConsumptionScreen\|smart_plug_consumption_screen" lib/ test/` -- returns nothing
4. Run `flutter test` -- full suite passes (test count decreases by the number of tests that were in the deleted test file)
5. Run `flutter analyze` -- clean
  </verify>
  <done>
- SmartPlugConsumptionScreen source file deleted
- SmartPlugConsumptionScreen test file deleted
- No remaining references to the deleted screen anywhere in codebase
- All remaining tests pass
  </done>
</task>

</tasks>

<verification>
1. `flutter test` -- full suite passes
2. `flutter analyze` -- clean (deprecated warnings eliminated)
3. `grep -r "buildGlassFAB\|GlassBottomNav\|SmartPlugConsumptionScreen" lib/ test/` -- returns zero matches
4. `grep -c "@Deprecated" lib/widgets/liquid_glass_widgets.dart` -- returns 0 (no deprecated annotations remain)
</verification>

<success_criteria>
- GlassBottomNav and buildGlassFAB fully removed from liquid_glass_widgets.dart with zero remaining references
- HouseholdsScreen and RoomsScreen use standard FloatingActionButton
- SmartPlugConsumptionScreen and its test are deleted
- All tests pass, flutter analyze is clean
- No deprecation annotations remain in liquid_glass_widgets.dart
</success_criteria>

<output>
After completion, create `.planning/phases/32-heating-analytics-cleanup/32-02-SUMMARY.md`
</output>
