---
phase: 21-smart-plug-screen-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/screens/smart_plugs_screen.dart
  - lib/screens/smart_plug_analytics_screen.dart
  - lib/providers/smart_plug_analytics_provider.dart
  - lib/l10n/app_en.arb
  - lib/l10n/app_de.arb
  - test/screens/smart_plugs_screen_test.dart
  - test/screens/smart_plug_analytics_screen_test.dart
  - test/providers/smart_plug_analytics_provider_test.dart
autonomous: true
requirements: [FR-19]

must_haves:
  truths:
    - "SmartPlugsScreen shows bottom nav with Analyse and Liste tabs"
    - "Default tab is Liste (index 1)"
    - "FAB visible on Liste tab, hidden on Analyse tab"
    - "No pie_chart icon in app bar (analytics is now in bottom nav)"
    - "Rooms button (meeting_room icon) remains in app bar"
    - "Analyse tab shows monthly-only analysis with no yearly SegmentedButton"
    - "Month navigation arrows move between months"
    - "Stats card shows renamed labels: Gesamtverbrauch, Davon erfasst, Nicht erfasst"
    - "UI order: month nav, stats card, Verbrauch nach Raum (title + pie + list with %), Verbrauch nach Steckdose (title + pie + list)"
    - "Room list items show kWh + percentage"
    - "Reduced padding between list items"
    - "All new strings localized in EN and DE"
  artifacts:
    - path: "lib/screens/smart_plugs_screen.dart"
      provides: "Bottom nav screen with IndexedStack (Analyse + Liste)"
      contains: "GlassBottomNav"
    - path: "lib/screens/smart_plug_analytics_screen.dart"
      provides: "Refactored inline Analyse tab content (no standalone Scaffold)"
      contains: "_MonthNavigation"
    - path: "lib/providers/smart_plug_analytics_provider.dart"
      provides: "Monthly-only analytics provider (no yearly code path)"
    - path: "lib/l10n/app_en.arb"
      provides: "EN localization for new/renamed keys"
      contains: "totalConsumptionLabel"
    - path: "lib/l10n/app_de.arb"
      provides: "DE localization for new/renamed keys"
      contains: "totalConsumptionLabel"
    - path: "test/screens/smart_plugs_screen_test.dart"
      provides: "Tests for bottom nav, FAB visibility, tab switching"
    - path: "test/screens/smart_plug_analytics_screen_test.dart"
      provides: "Tests for renamed stats, percentage display, UI order, monthly-only"
  key_links:
    - from: "lib/screens/smart_plugs_screen.dart"
      to: "lib/screens/smart_plug_analytics_screen.dart"
      via: "IndexedStack child calling analysis build method"
      pattern: "_buildAnalyseTab"
    - from: "lib/screens/smart_plugs_screen.dart"
      to: "lib/widgets/liquid_glass_widgets.dart"
      via: "GlassBottomNav and buildGlassFAB"
      pattern: "GlassBottomNav"
    - from: "lib/screens/smart_plug_analytics_screen.dart"
      to: "lib/providers/smart_plug_analytics_provider.dart"
      via: "context.watch for analytics data"
      pattern: "context.watch<SmartPlugAnalyticsProvider>"
---

<objective>
Overhaul the Smart Plug screen to match the unified list/analysis bottom navigation pattern (Phases 19/20). Add GlassBottomNav with Analyse | Liste tabs, make analytics monthly-only, rename stats, reorder UI sections, add percentages to room breakdown, and fully localize.

Purpose: Consistent navigation UX across all meter screens; smart plug analytics become an inline tab instead of a separate screen navigation.
Output: Refactored SmartPlugsScreen with bottom nav, simplified analytics as Analyse tab, comprehensive tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21/CONTEXT.md

@lib/screens/smart_plugs_screen.dart
@lib/screens/smart_plug_analytics_screen.dart
@lib/screens/electricity_screen.dart
@lib/providers/smart_plug_analytics_provider.dart
@lib/services/analytics/analytics_models.dart
@lib/l10n/app_en.arb
@lib/l10n/app_de.arb
@test/screens/smart_plugs_screen_test.dart
@test/screens/smart_plug_analytics_screen_test.dart
@lib/widgets/liquid_glass_widgets.dart

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/widgets/liquid_glass_widgets.dart:
```dart
class GlassBottomNav extends StatelessWidget {
  final int currentIndex;
  final ValueChanged<int> onTap;
  final List<BottomNavigationBarItem> items;
}
PreferredSizeWidget buildGlassAppBar({required BuildContext context, required String title, List<Widget>? actions});
Widget buildGlassFAB({required BuildContext context, required IconData icon, required VoidCallback onPressed});
```

From lib/services/analytics/analytics_models.dart:
```dart
enum AnalyticsPeriod { monthly, yearly }
class SmartPlugAnalyticsData {
  final List<PlugConsumption> byPlug;
  final List<RoomConsumption> byRoom;
  final double totalSmartPlug;
  final double? totalElectricity;
  final double? otherConsumption;
  final String unit; // always 'kWh'
}
class PlugConsumption { plugId, plugName, roomName, consumption, color }
class RoomConsumption { roomId, roomName, consumption, color }
class PieSliceData { label, value, percentage, color }
```

From lib/providers/smart_plug_analytics_provider.dart:
```dart
class SmartPlugAnalyticsProvider extends ChangeNotifier {
  SmartPlugAnalyticsData? get data;
  bool get isLoading;
  AnalyticsPeriod get period;
  DateTime get selectedMonth;
  int get selectedYear;
  void setPeriod(AnalyticsPeriod period);
  void navigateMonth(int delta);
  void navigateYear(int delta);
  Future<void> loadData();
}
```

Bottom nav pattern (from ElectricityScreen / GasScreen):
```dart
// StatefulWidget with _currentTab = 1 (default Liste)
body: IndexedStack(
  index: _currentTab,
  children: [_buildAnalyseTab(context), _buildListeTab(context)],
),
floatingActionButton: _currentTab == 1 ? buildGlassFAB(...) : null,
bottomNavigationBar: GlassBottomNav(
  currentIndex: _currentTab,
  onTap: (index) => setState(() => _currentTab = index),
  items: [
    BottomNavigationBarItem(icon: Icon(Icons.analytics), label: l10n.analysis),
    BottomNavigationBarItem(icon: Icon(Icons.list), label: l10n.list),
  ],
),
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add l10n keys, simplify provider to monthly-only, refactor SmartPlugsScreen with bottom nav + inline Analyse tab</name>
  <files>
    lib/l10n/app_en.arb
    lib/l10n/app_de.arb
    lib/providers/smart_plug_analytics_provider.dart
    lib/screens/smart_plugs_screen.dart
    lib/screens/smart_plug_analytics_screen.dart
    test/providers/smart_plug_analytics_provider_test.dart
  </files>
  <behavior>
    - SmartPlugsScreen renders GlassBottomNav with 2 items (Analyse, Liste)
    - Default tab is Liste (index 1)
    - FAB visible on Liste tab only, hidden on Analyse tab
    - No pie_chart IconButton in app bar
    - meeting_room IconButton remains in app bar
    - Analyse tab shows month navigation (chevron_left / chevron_right, month label)
    - No SegmentedButton for period selection (monthly-only)
    - Stats card shows renamed labels: "Total Consumption" / "Tracked by Plugs" / "Not Tracked" in EN
    - Stats card shows renamed labels: "Gesamtverbrauch" / "Davon erfasst" / "Nicht erfasst" in DE
    - UI order in Analyse tab: month nav -> stats card -> "Verbrauch nach Raum" section (title, pie, list with kWh + %) -> "Verbrauch nach Steckdose" section (title, pie, list)
    - Room breakdown items display "X.X kWh (YY%)" format
    - Reduced padding between breakdown list items (dense ListTile or custom with reduced vertical padding)
  </behavior>
  <action>
    **Step 1 — Localization keys (app_en.arb + app_de.arb):**
    Add new l10n keys and update existing ones:
    - `totalConsumptionLabel`: EN="Total Consumption", DE="Gesamtverbrauch" — displays `totalElectricity` data (total from electricity meter, was "Total Electricity")
    - `trackedByPlugs`: EN="Tracked by Plugs", DE="Davon erfasst" — displays `totalSmartPlug` data (sum of plug readings, was "Total Tracked")
    - `notTracked`: EN="Not Tracked", DE="Nicht erfasst" — displays `otherConsumption` data (difference, was "Other (Untracked)")
    - `consumptionByRoomTitle`: EN="Consumption by Room", DE="Verbrauch nach Raum"
    - `consumptionByPlugTitle`: EN="Consumption by Plug", DE="Verbrauch nach Steckdose"
    - `consumptionWithPercent`: EN="{value} kWh ({percent}%)", DE="{value} kWh ({percent}%)" with placeholders `value: String, percent: String`
    Keep existing keys (`consumptionByPlug`, `consumptionByRoom`, `totalTracked`, `totalElectricity`, `otherConsumption`) in the ARB files for backward compatibility but the screen will now use the new keys.
    Run `flutter gen-l10n` after editing ARB files.

    **Step 2 — Simplify SmartPlugAnalyticsProvider to monthly-only:**
    In `lib/providers/smart_plug_analytics_provider.dart`:
    - Remove `_period` field, `period` getter, and `setPeriod()` method entirely
    - Remove `_selectedYear` field, `selectedYear` getter, `setSelectedYear()`, and `navigateYear()` methods
    - In `loadData()`, remove the `switch (_period)` block. Always compute date range as monthly:
      ```dart
      final from = _selectedMonth;
      final to = DateTime(_selectedMonth.year, _selectedMonth.month + 1, 1);
      ```
    - Remove the `AnalyticsPeriod.yearly` case entirely from loadData
    - Keep the `AnalyticsPeriod` enum in analytics_models.dart (it is NOT used elsewhere in production code, but removing it would break test imports — leave it for now; it can be cleaned up later if needed)

    **Step 2b — Update provider tests (test/providers/smart_plug_analytics_provider_test.dart):**
    Since `setPeriod()`, `navigateYear()`, `setSelectedYear()`, `period` getter, and `selectedYear` getter are removed:
    - Remove tests in 'initial state' group that check `provider.period` and `provider.selectedYear`
    - Remove the entire `group('period switching', ...)` block
    - Remove the entire `group('year navigation', ...)` block
    - Keep all 'month navigation', 'loadData', and 'setHouseholdId' tests intact

    **Step 3 — Refactor SmartPlugsScreen with bottom nav (following electricity/gas pattern):**
    Convert `SmartPlugsScreen` from `StatelessWidget` to `StatefulWidget`:
    - Add `int _currentTab = 1;` state (0=Analyse, 1=Liste, default Liste)
    - In `initState`, use `WidgetsBinding.instance.addPostFrameCallback` to call `context.read<SmartPlugAnalyticsProvider>().loadData()` to preload analytics
    - Build method returns Scaffold with:
      - `appBar`: buildGlassAppBar with title `l10n.smartPlugs`, actions = ONLY the meeting_room IconButton (remove pie_chart icon entirely)
      - `body`: IndexedStack with index `_currentTab`, children: `[_buildAnalyseTab(context), _buildListeTab(context)]`
      - `floatingActionButton`: `_currentTab == 1 ? buildGlassFAB(...) : null`
      - `bottomNavigationBar`: `GlassBottomNav(currentIndex: _currentTab, onTap: (i) => setState(() => _currentTab = i), items: [BottomNavigationBarItem(icon: Icon(Icons.analytics), label: l10n.analysis), BottomNavigationBarItem(icon: Icon(Icons.list), label: l10n.list)])`
    - `_buildListeTab` returns the existing plug list content (plugsByRoom empty check, _SmartPlugsList widget, empty state)
    - `_buildAnalyseTab` returns the refactored analytics content (see Step 4)
    - Keep all existing private widgets (_SmartPlugsList, _RoomSection, _SmartPlugCard) intact in the file

    **Step 4 — Refactor SmartPlugAnalyticsScreen into inline Analyse tab content:**
    Rename `SmartPlugAnalyticsScreen` to `SmartPlugAnalyseTab` in `lib/screens/smart_plug_analytics_screen.dart`. Remove its Scaffold and AppBar wrapper — it should be a plain StatelessWidget returning the analytics content directly (suitable as IndexedStack child):
    - Create a new widget `SmartPlugAnalyseTab` (StatelessWidget) that contains all the analytics content WITHOUT Scaffold/AppBar
    - Remove `_PeriodSelector` widget entirely (no monthly/yearly toggle)
    - Remove `_PeriodNavigationHeader` widget (which switches between month/year nav)
    - Remove `_YearNavigation` widget entirely
    - Keep `_MonthNavigation` widget (month arrows with label)
    - Restructure `_buildDataSections` to match new UI order:
      1. `_MonthNavigation` (month arrows)
      2. Stats card (`_SummaryCard`) — update labels to use new l10n keys: `l10n.totalConsumptionLabel` for totalElectricity (was "Total Electricity" showing total from meter), `l10n.trackedByPlugs` for totalSmartPlug (was "Total Tracked"), `l10n.notTracked` for otherConsumption (was "Other (Untracked)")
         IMPORTANT: The stat meaning mapping is: "Gesamtverbrauch" = totalElectricity (total from electricity meter), "Davon erfasst" = totalSmartPlug (tracked by plugs), "Nicht erfasst" = otherConsumption (difference). Swap the first two summary rows accordingly.
      3. "Verbrauch nach Raum" section: title (`l10n.consumptionByRoomTitle`), ConsumptionPieChart with room slices, room breakdown list with kWh + percentage
      4. "Verbrauch nach Steckdose" section: title (`l10n.consumptionByPlugTitle`), ConsumptionPieChart with plug slices, plug breakdown list
    - Update `_RoomBreakdownItem` to show percentage: calculate `percentage = (room.consumption / totalSmartPlug) * 100` and display trailing as `l10n.consumptionWithPercent(formattedKwh, formattedPercent)` — e.g., "12.5 kWh (34%)"
    - Apply reduced padding to breakdown items: use `visualDensity: VisualDensity.compact` on ListTile or `contentPadding: EdgeInsets.symmetric(horizontal: 16, vertical: 0)` with `dense: true`
    - Import and use `SmartPlugAnalyseTab` in SmartPlugsScreen._buildAnalyseTab

    **Step 5 — Run flutter gen-l10n + flutter analyze:**
    ```bash
    flutter gen-l10n
    flutter analyze
    ```
    Fix any issues.
  </action>
  <verify>
    <automated>flutter analyze && flutter test test/screens/smart_plugs_screen_test.dart test/screens/smart_plug_analytics_screen_test.dart test/providers/smart_plug_analytics_provider_test.dart</automated>
  </verify>
  <done>
    - SmartPlugsScreen is a StatefulWidget with GlassBottomNav (Analyse | Liste), IndexedStack, conditional FAB
    - No pie_chart icon in app bar; meeting_room icon remains
    - Analyse tab renders monthly-only analysis (no SegmentedButton, no yearly nav)
    - Stats card uses renamed labels matching l10n keys
    - UI order: month nav > stats > room section (title + pie + list with %) > plug section (title + pie + list)
    - Room breakdown items show "X.X kWh (YY%)"
    - Reduced padding on breakdown list items
    - All strings localized (EN + DE)
    - flutter analyze passes with zero issues
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Comprehensive tests for bottom nav, FAB visibility, renamed stats, percentages, UI order, and monthly-only mode</name>
  <files>
    test/screens/smart_plugs_screen_test.dart
    test/screens/smart_plug_analytics_screen_test.dart
  </files>
  <behavior>
    - Bottom nav renders with "Analysis" and "List" labels + GlassBottomNav widget
    - Default tab is Liste showing plug list (or empty state)
    - Tapping "Analysis" switches to Analyse tab content
    - FAB visible on Liste tab (FloatingActionButton found)
    - FAB hidden on Analyse tab (FloatingActionButton not found)
    - No pie_chart icon in app bar
    - meeting_room icon present in app bar
    - No SegmentedButton widget rendered (monthly-only)
    - Month navigation arrows present (chevron_left, chevron_right)
    - Stats card shows "Total Consumption" (or EN equivalent), "Tracked by Plugs", "Not Tracked"
    - Stats card does NOT show old labels "Total Tracked", "Total Electricity", "Other (Untracked)"
    - Room breakdown items contain percentage text matching pattern "XX%"
    - UI section order: month nav appears before stats, stats before room section, room section before plug section
    - Tab state preserved when switching (IndexedStack behavior)
  </behavior>
  <action>
    **Update test/screens/smart_plugs_screen_test.dart:**
    The SmartPlugsScreen now needs `SmartPlugAnalyticsProvider` in the provider tree for the Analyse tab.
    - Add `SmartPlugAnalyticsProvider` (or a mock) to `wrapWithProviders`. Since SmartPlugsScreen is a StatefulWidget that uses `context.read<SmartPlugAnalyticsProvider>()` in initState, use a mock provider with mocktail (like the existing analytics test does).
    - Add to the existing wrapWithProviders: `ChangeNotifierProvider<SmartPlugAnalyticsProvider>.value(value: mockAnalyticsProvider)`
    - Create `MockSmartPlugAnalyticsProvider` at top of file using mocktail pattern from existing analytics test
    - Set up stubs: `isLoading=false`, `data=null`, `selectedMonth=DateTime(2026,3,1)`
    - Update existing test "has analytics and rooms buttons in app bar" to expect NO pie_chart icon, but DO expect meeting_room icon
    - Add new tests in a "Bottom Navigation" group:
      - "renders bottom nav with Analysis and List labels" — find text 'Analysis', 'List', find GlassBottomNav
      - "default tab is Liste" — on default load, find plug list content or empty state
      - "tapping Analysis switches to Analyse tab" — tap 'Analysis', pump, verify analytics content appears
      - "FAB visible on Liste tab" — find FloatingActionButton
      - "FAB hidden on Analyse tab" — tap 'Analysis', pump, expect FloatingActionButton findsNothing
      - "no pie_chart icon in app bar" — expect find.byIcon(Icons.pie_chart) findsNothing
      - "rooms button remains in app bar" — expect find.byIcon(Icons.meeting_room) findsOneWidget
    - Update "FAB shows snackbar when no rooms exist" to account for FAB only on Liste tab (should still work since default is Liste)

    **Update test/screens/smart_plug_analytics_screen_test.dart:**
    The SmartPlugAnalyticsScreen is now `SmartPlugAnalyseTab` (or whatever the refactored widget is named). Update imports and widget references.
    - Update `buildSubject()` to wrap `SmartPlugAnalyseTab` in a Scaffold (since it no longer has its own Scaffold)
    - Remove test for "renders AppBar with title Smart Plug Analytics" (no longer has AppBar)
    - Remove test for "renders SegmentedButton with two segments (Monthly, Yearly)"
    - Remove test for "tapping Yearly segment calls setPeriod"
    - Remove test for "renders year navigation header when period is yearly"
    - Remove stubs for `period` and `selectedYear` and `setPeriod` (provider no longer has these)
    - Update `setUpDefaultStubs` to remove `period` parameter (provider only does monthly now)
    - Add tests:
      - "does not render SegmentedButton" — findsNothing for SegmentedButton widget
      - "renders month navigation with arrows" — find chevron_left, chevron_right, month text
      - "stats card shows renamed labels" — with sample data, find 'Total Consumption', 'Tracked by Plugs', 'Not Tracked'
      - "stats card does NOT show old labels" — findsNothing for 'Total Tracked', 'Total Electricity', 'Other (Untracked)'
      - "section titles use new l10n keys" — find 'Consumption by Room' and 'Consumption by Plug' (these replace the old 'Room Breakdown' and 'Plug Breakdown' section titles; update any existing tests that check for the old titles)
      - "room breakdown shows kWh and percentage" — with sample data (Office=35.0/50.0=70%), find text matching "70%" pattern
      - "UI sections appear in correct order" — use tester to verify month nav comes before stats card, stats before room section, room before plug section. Use `find.text(...).evaluate().first.renderObject` to compare vertical positions, or just verify all expected section titles exist in order by scrolling.
      - "reduced padding on list items" — verify ListTile with `dense: true` or custom compact layout
    - Keep tests that still apply: loading indicator, empty state, month nav tap calls navigateMonth, consumption sections exist, plug/room breakdown lists
  </action>
  <verify>
    <automated>flutter test test/screens/smart_plugs_screen_test.dart test/screens/smart_plug_analytics_screen_test.dart</automated>
  </verify>
  <done>
    - All bottom nav tests pass: GlassBottomNav renders, default Liste, tab switching works
    - FAB visibility tests pass: visible on Liste, hidden on Analyse
    - App bar tests pass: no pie_chart, meeting_room present
    - Monthly-only tests pass: no SegmentedButton, month arrows present
    - Renamed stats tests pass: new labels found, old labels absent
    - Percentage tests pass: room items show "kWh (XX%)" format
    - UI order tests pass: sections in correct sequence
    - All existing still-valid tests continue to pass
    - Full test suite (`flutter test`) passes with zero failures
  </done>
</task>

<task type="auto">
  <name>Task 3: Full test suite validation and flutter analyze</name>
  <files></files>
  <action>
    Run the complete test suite and static analysis to ensure nothing is broken:
    1. `flutter test` — all ~1069+ tests must pass
    2. `flutter analyze` — zero issues
    If any failures, debug and fix iteratively (Ralph Loop) until both commands pass cleanly.
    Common issues to watch for:
    - Existing tests that reference `SmartPlugAnalyticsScreen` directly (as a standalone navigated screen) may need updating if any screen navigates to it
    - Provider test (`test/providers/smart_plug_analytics_provider_test.dart`) may need updates since `setPeriod`, `selectedYear`, `navigateYear` methods were removed
    - Any test that checks for `Icons.pie_chart` on the SmartPlugsScreen will now fail (expected — the icon was removed)
  </action>
  <verify>
    <automated>flutter analyze && flutter test</automated>
  </verify>
  <done>
    - `flutter test` passes all tests (1069+ including new tests for this phase)
    - `flutter analyze` reports zero issues
    - Phase 21 is complete
  </done>
</task>

</tasks>

<verification>
1. `flutter analyze` — zero issues
2. `flutter test` — all tests pass (1069+ baseline plus new phase 21 tests)
3. SmartPlugsScreen renders bottom nav with Analyse | Liste tabs, defaults to Liste
4. FAB appears on Liste tab only
5. No pie_chart icon in app bar
6. Analyse tab shows monthly-only analysis (no yearly toggle)
7. Stats renamed: Gesamtverbrauch / Davon erfasst / Nicht erfasst
8. Room breakdown shows kWh + percentage
9. UI order correct: month nav > stats > room section > plug section
</verification>

<success_criteria>
- SmartPlugsScreen matches the bottom nav pattern from electricity/gas screens
- Analytics is inline as Analyse tab (no separate screen navigation)
- Monthly-only analysis (SegmentedButton removed)
- Stats labels renamed per requirements
- Room items show percentage
- All strings localized EN + DE
- Full test suite green (flutter test + flutter analyze)
</success_criteria>

<output>
After completion, create `.planning/phases/21/21-01-SUMMARY.md`
</output>
