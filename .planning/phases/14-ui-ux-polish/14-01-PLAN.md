---
phase: 14-ui-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/providers/locale_provider.dart
  - lib/services/number_format_service.dart
  - lib/l10n/app_de.arb
  - lib/l10n/app_en.arb
  - lib/app_theme.dart
  - test/providers/locale_provider_test.dart
  - test/services/number_format_service_test.dart
autonomous: true
requirements:
  - FR-12.2.1
  - FR-12.2.2
  - FR-12.2.3
  - FR-12.2.4
  - FR-12.4.1
  - FR-12.7.1
  - FR-12.7.2
  - FR-12.7.3

must_haves:
  truths:
    - "ValtraNumberFormat.consumption(1234.5, 'de') returns '1.234,5'"
    - "ValtraNumberFormat.consumption(1234.5, 'en') returns '1,234.5'"
    - "ValtraNumberFormat.time(DateTime(2026,3,7,9,43), 'de') returns '9:43 Uhr'"
    - "LocaleProvider persists locale to SharedPreferences and restores on init"
    - "LocaleProvider.setLocale notifies listeners immediately"
    - "All German ARB strings use proper umlauts (no ae/oe/ue)"
    - "Dark theme onSecondary uses black text on Lemon Chiffon backgrounds"
  artifacts:
    - path: "lib/providers/locale_provider.dart"
      provides: "In-app language toggle state management"
      exports: ["LocaleProvider"]
    - path: "lib/services/number_format_service.dart"
      provides: "Centralized locale-aware number and time formatting"
      exports: ["ValtraNumberFormat"]
    - path: "lib/l10n/app_de.arb"
      provides: "German localization with correct umlauts"
      contains: "Aufschl\\u00fcsselung"
    - path: "test/providers/locale_provider_test.dart"
      provides: "Unit tests for locale provider"
      min_lines: 50
    - path: "test/services/number_format_service_test.dart"
      provides: "Unit tests for number formatting"
      min_lines: 60
  key_links:
    - from: "lib/providers/locale_provider.dart"
      to: "SharedPreferences"
      via: "getString/setString('app_locale')"
      pattern: "SharedPreferences.*app_locale"
    - from: "lib/services/number_format_service.dart"
      to: "intl NumberFormat"
      via: "NumberFormat constructor with locale parameter"
      pattern: "NumberFormat.*locale"
---

<objective>
Create the foundational utilities for Phase 14: LocaleProvider for in-app language toggle, ValtraNumberFormat for centralized locale-aware number/time formatting, fix all umlaut encoding in German ARB file, and fix dark mode text color on accent backgrounds.

Purpose: These are the building blocks that every subsequent plan depends on. LocaleProvider provides the locale string that ValtraNumberFormat and all screens use. Umlaut fixes must happen in the ARB source files before any screen-level work. The dark mode fix is a one-line theme change that unblocks all glass widget work.

Output: 2 new source files, 2 new test files, 2 modified ARB files, 1 modified theme file, regenerated l10n Dart files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ui-ux-polish/14-RESEARCH.md

@lib/providers/theme_provider.dart
@lib/app_theme.dart
@lib/l10n/app_de.arb
@lib/l10n/app_en.arb
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/providers/theme_provider.dart (pattern to follow for LocaleProvider):
```dart
class ThemeProvider extends ChangeNotifier {
  static const String _themeModeKey = 'theme_mode';
  SharedPreferences? _prefs;
  ThemeMode _themeMode = ThemeMode.system;

  Future<void> init() async { ... }
  Future<void> setThemeMode(ThemeMode mode) async { ... }
  bool isDark(BuildContext context) { ... }
}
```

From lib/app_theme.dart (dark theme line to fix):
```dart
// Line 97 - current:
onSecondary: AppColors.darkOnSurface,
// Must change to:
onSecondary: Colors.black,  // Black text on Lemon Chiffon accent backgrounds
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LocaleProvider and ValtraNumberFormat with tests</name>
  <files>
    lib/providers/locale_provider.dart,
    lib/services/number_format_service.dart,
    test/providers/locale_provider_test.dart,
    test/services/number_format_service_test.dart
  </files>
  <behavior>
    - LocaleProvider.init() with no saved pref: locale is null (follow device)
    - LocaleProvider.init() with saved 'de': locale is Locale('de')
    - LocaleProvider.setLocale(Locale('en')): saves to prefs, notifies listeners
    - LocaleProvider.localeString: returns 'de' when locale is Locale('de'), 'de' when null (default)
    - ValtraNumberFormat.consumption(1234.5, 'de') returns '1.234,5'
    - ValtraNumberFormat.consumption(1234.5, 'en') returns '1,234.5'
    - ValtraNumberFormat.waterReading(12.345, 'de') returns '12,345'
    - ValtraNumberFormat.waterReading(12.345, 'en') returns '12.345'
    - ValtraNumberFormat.currency(78.5, 'de') returns '78,50'
    - ValtraNumberFormat.currency(78.5, 'en') returns '78.50'
    - ValtraNumberFormat.time(DateTime(2026,3,7,9,43), 'de') returns '9:43 Uhr'
    - ValtraNumberFormat.time(DateTime(2026,3,7,9,43), 'en') returns '09:43'
    - ValtraNumberFormat.date(DateTime(2026,3,7), 'de') returns German month name (Marz)
    - ValtraNumberFormat.date(DateTime(2026,3,7), 'en') returns English month name (March)
  </behavior>
  <action>
    1. Create `lib/providers/locale_provider.dart`:
       - Follow ThemeProvider pattern (ChangeNotifier + SharedPreferences)
       - Key: 'app_locale', stores language code string ('de' or 'en')
       - `Locale? _locale` (null = follow device, default German)
       - `Future<void> init()` — load from SharedPreferences
       - `Future<void> setLocale(Locale locale)` — persist and notify
       - `String get localeString` — returns languageCode or 'de' if null
       - `Locale? get locale` — for MaterialApp.locale binding

    2. Create `lib/services/number_format_service.dart`:
       - Static utility class `ValtraNumberFormat`
       - `static String consumption(double value, String locale)` — 1 decimal place, locale-aware
       - `static String waterReading(double value, String locale)` — 3 decimal places, locale-aware
       - `static String currency(double value, String locale)` — 2 decimal places, locale-aware
       - `static String time(DateTime dt, String locale)` — German: "H:mm Uhr", English: "HH:mm"
       - `static String date(DateTime dt, String locale)` — uses DateFormat.yMMMM(locale)
       - `static String monthYear(DateTime dt, String locale)` — uses DateFormat.yMMMM(locale)
       - All use `NumberFormat` and `DateFormat` from `intl` package with locale parameter

    3. Write comprehensive tests for both (TDD: write tests first, then implement).
       - Use `SharedPreferences.setMockInitialValues()` for locale provider tests
       - Test all locale combinations (de, en) for number format
       - Test edge cases: zero, negative, very large numbers, midnight time
  </action>
  <verify>
    <automated>flutter test test/providers/locale_provider_test.dart test/services/number_format_service_test.dart --no-pub</automated>
  </verify>
  <done>
    LocaleProvider and ValtraNumberFormat exist with full test coverage.
    LocaleProvider persists/restores locale via SharedPreferences.
    ValtraNumberFormat formats numbers, currency, time, and dates correctly for both DE and EN locales.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix umlaut encoding in ARB files and dark mode accent text color</name>
  <files>
    lib/l10n/app_de.arb,
    lib/l10n/app_en.arb,
    lib/app_theme.dart
  </files>
  <action>
    1. Fix all umlaut strings in `lib/l10n/app_de.arb`:
       Replace every occurrence of ae/oe/ue with proper umlauts in these known strings:
       - "Aufschluesselung" -> "Aufschlüsselung" (2 occurrences)
       - "fuer" -> "für" (2 occurrences)
       - "Jaehrlich" -> "Jährlich"
       - "Zaehlereinstellungen" -> "Zählereinstellungen"
       - "Ueber" -> "Über"
       - "gueltige" -> "gültige"
       - "Grundgebuehr" -> "Grundgebühr" (2 occurrences)
       - "hinzufuegen" -> "hinzufügen"
       - "Gueltig" -> "Gültig"
       - "Waehrung" -> "Währung"
       - "Geschaetzte" -> "Geschätzte"
       - "Jaehrliche" -> "Jährliche"
       - "loeschen" -> "löschen" (2 occurrences)
       - "Kostenuebersicht" -> "Kostenübersicht"
       - "geloescht" -> "gelöscht"
       Scan the ENTIRE file for any other ae/oe/ue patterns that should be umlauts.

    2. Add new localization keys to both ARB files:
       - `monthlyProgress`: DE "Monatsverlauf", EN "Monthly Progress" (replaces "Monatsvergleich")
       - `language`: DE "Sprache", EN "Language"
       - `languageDE`: DE "Deutsch", EN "German"
       - `languageEN`: DE "Englisch", EN "English"

    3. Run `flutter gen-l10n` to regenerate the Dart localization files.

    4. Fix dark mode accent text color in `lib/app_theme.dart`:
       - Line 97: Change `onSecondary: AppColors.darkOnSurface` to `onSecondary: Colors.black`
       - This ensures black text on Lemon Chiffon (yellow) accent backgrounds in dark mode

    5. Run `flutter analyze` to verify no issues.
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter analyze --no-pub && flutter test --no-pub</automated>
  </verify>
  <done>
    All German strings in app_de.arb use proper umlauts (zero ae/oe/ue patterns remaining).
    New localization keys (monthlyProgress, language, languageDE, languageEN) present in both ARB files.
    Dark theme onSecondary is Colors.black for readable text on Lemon Chiffon backgrounds.
    flutter analyze reports zero issues. All existing tests pass.
  </done>
</task>

</tasks>

<verification>
- `flutter test --no-pub` — all existing 707+ tests pass plus new locale/format tests
- `flutter analyze --no-pub` — zero issues
- Grep for `ae|oe|ue` patterns in app_de.arb confirms no remaining umlaut issues (excluding legitimate German words like "neue", "Haus", etc.)
</verification>

<success_criteria>
- LocaleProvider created following ThemeProvider pattern, with SharedPreferences persistence
- ValtraNumberFormat provides consumption(), waterReading(), currency(), time(), date() methods
- All 18+ umlaut strings fixed in app_de.arb
- 4 new l10n keys added (monthlyProgress, language, languageDE, languageEN)
- Dark theme accent text is black (readable on Lemon Chiffon)
- All tests pass, flutter analyze clean
</success_criteria>

<output>
After completion, create `.planning/phases/14-ui-ux-polish/14-01-SUMMARY.md`
</output>
