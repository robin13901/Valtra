---
phase: 14-ui-ux-polish
plan: 06
type: execute
wave: 3
depends_on: ["14-03"]
files_modified:
  - lib/screens/monthly_analytics_screen.dart
  - lib/screens/smart_plug_analytics_screen.dart
  - lib/providers/analytics_provider.dart
  - lib/providers/smart_plug_analytics_provider.dart
  - lib/services/analytics/analytics_models.dart
  - lib/l10n/app_de.arb
  - lib/l10n/app_en.arb
autonomous: true
requirements:
  - FR-12.6.1
  - FR-12.6.2
  - FR-12.6.3
  - FR-12.6.4
  - FR-12.6.5

must_haves:
  truths:
    - "No daily trends section appears in monthly analytics screen"
    - "No custom date range picker (calendar icon) appears in any analytics app bar"
    - "No 'Benutzerdefiniert' tab appears in smart plug analytics"
    - "Monthly analytics tab reads 'Monatsverlauf' (DE) / 'Monthly Progress' (EN)"
    - "Analysis screens default to current month when opened"
    - "AnalyticsPeriod enum has no 'custom' value"
  artifacts:
    - path: "lib/screens/monthly_analytics_screen.dart"
      provides: "Monthly analytics without daily view or custom date range"
    - path: "lib/screens/smart_plug_analytics_screen.dart"
      provides: "Smart plug analytics without custom period tab"
    - path: "lib/providers/analytics_provider.dart"
      provides: "Analytics provider without custom range support"
    - path: "lib/services/analytics/analytics_models.dart"
      provides: "AnalyticsPeriod enum without custom value"
  key_links:
    - from: "lib/screens/monthly_analytics_screen.dart"
      to: "lib/providers/analytics_provider.dart"
      via: "No more setCustomRange calls"
      pattern: "setCustomRange"
---

<objective>
Clean up analysis screens: remove daily trends view, remove custom date range feature, remove "Benutzerdefiniert" tab from smart plug analytics, rename "Monatsvergleich" to "Monatsverlauf", and ensure analysis screens default to current month.

Purpose: FR-12.6 simplifies the analytics UX. The app only has monthly readings, so daily views are misleading. Custom date ranges add complexity without value. These removals clean up the analytics layer.

Output: 2 screen files, 2 provider files, 1 model file, 2 ARB files modified. Simpler analytics UX.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-ui-ux-polish/14-RESEARCH.md

@lib/screens/monthly_analytics_screen.dart
@lib/screens/smart_plug_analytics_screen.dart
@lib/providers/analytics_provider.dart
@lib/providers/smart_plug_analytics_provider.dart
@lib/services/analytics/analytics_models.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove daily view and custom date range from analytics screens</name>
  <files>
    lib/screens/monthly_analytics_screen.dart,
    lib/screens/smart_plug_analytics_screen.dart
  </files>
  <action>
    **monthly_analytics_screen.dart:**
    1. REMOVE the custom date range `IconButton` (calendar icon) from app bar actions (~lines 34-38)
    2. REMOVE the "Daily Trends" section entirely:
       - Remove the section title (e.g., "Tagesverlauf" heading)
       - Remove the `ConsumptionLineChart` widget and its `ChartLegend`
       - Remove the import for ConsumptionLineChart if no longer used
       - Remove the import for ChartLegend if no longer used
    3. RENAME: Change `l10n.monthlyComparison` to `l10n.monthlyProgress` (the new key added in Plan 01)
    4. REMOVE the `_pickDateRange()` method entirely
    5. REMOVE any references to `customRange` in the `_MonthNavigationHeader` widget
    6. Verify the screen still shows: month navigation, consumption bar chart, and cost summary (if configured)

    **smart_plug_analytics_screen.dart:**
    1. REMOVE the `AnalyticsPeriod.custom` `ButtonSegment` from the period selector (~lines 233-235)
    2. REMOVE the custom date range `IconButton` (calendar icon) from app bar actions (~lines 23-29)
    3. REMOVE the `_pickDateRange()` method (~lines 189-206)
    4. REMOVE the `_CustomRangeDisplay` widget class (~lines 331-361) and any usage
    5. The period selector should only show: Monthly and Yearly segments

    **FR-12.6.5 — Analysis screens default to current month:**
    - Verify that when the screen opens, it shows the current month (analytics_provider already defaults to current month on line 32, but verify all entry points set this correctly)
    - If any screen overrides the default month on init, remove that override
  </action>
  <verify>
    <automated>flutter analyze --no-pub && flutter test --no-pub</automated>
  </verify>
  <done>
    Monthly analytics: no daily trends section, no custom date range icon, uses "monthlyProgress" label.
    Smart plug analytics: no "Benutzerdefiniert" tab, no custom date range icon, no custom range display.
    Both screens default to current month. All tests pass, flutter analyze clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean up providers and models for removed features</name>
  <files>
    lib/providers/analytics_provider.dart,
    lib/providers/smart_plug_analytics_provider.dart,
    lib/services/analytics/analytics_models.dart,
    lib/l10n/app_de.arb,
    lib/l10n/app_en.arb
  </files>
  <action>
    **analytics_provider.dart:**
    1. Remove the `_customRange` field (line ~34)
    2. Remove the `customRange` getter (line ~65)
    3. Remove the `setCustomRange()` method (line ~98)
    4. In `_loadMonthlyData()`: Remove the conditional that uses `_customRange` for line chart start/end dates (~lines 234-236)
    5. In `_loadYearlyData()`: Remove any `_customRange` references (~lines 299+)
    6. Remove the `_customRange = null;` reset in setHouseholdId and setSelectedMonth methods
    7. Remove daily values data loading from `_loadMonthlyData()` if it was only used by the daily trends chart
       - Check if `dailyValues` field in `MonthlyAnalyticsData` is used elsewhere; if not, stop populating it

    **smart_plug_analytics_provider.dart:**
    1. Remove any custom period/date range support
    2. Remove `setCustomRange()` if it exists
    3. Ensure only monthly and yearly periods are supported

    **analytics_models.dart:**
    1. Remove `custom` from `enum AnalyticsPeriod { monthly, yearly, custom }` -> `enum AnalyticsPeriod { monthly, yearly }`
    2. Fix any compilation errors from removed enum value (switch statements, etc.)

    **ARB files (both):**
    1. Rename `monthlyComparison` key to keep it but also ensure `monthlyProgress` key exists (added in Plan 01)
    2. If `monthlyComparison` is still referenced elsewhere, keep it; if not, remove it
    3. Run `flutter gen-l10n` to regenerate

    **Update existing tests:**
    - Update analytics_provider_test.dart if it tests setCustomRange or custom period
    - Update smart_plug_analytics_provider_test.dart similarly
    - Update screen tests that verify daily view or custom range UI elements
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter analyze --no-pub && flutter test --no-pub</automated>
  </verify>
  <done>
    AnalyticsPeriod enum is { monthly, yearly } only.
    AnalyticsProvider has no _customRange field or setCustomRange method.
    SmartPlugAnalyticsProvider has no custom period support.
    Daily data loading removed from provider if unused.
    All tests updated and passing. flutter analyze clean.
  </done>
</task>

</tasks>

<verification>
- `flutter test --no-pub` — all tests pass
- `flutter analyze --no-pub` — zero issues
- Grep for `_customRange|setCustomRange` in lib/ returns zero results
- Grep for `AnalyticsPeriod.custom` in lib/ returns zero results
- Grep for `dailyTrends|Tagesverlauf|ConsumptionLineChart` in monthly_analytics_screen.dart returns zero
</verification>

<success_criteria>
- No daily view in monthly analytics
- No custom date range feature in any analytics screen
- No "Benutzerdefiniert" tab in smart plug analytics
- "Monatsvergleich" renamed to "Monatsverlauf"
- Analysis screens default to current month
- AnalyticsPeriod.custom removed from enum
- All tests pass, flutter analyze clean
</success_criteria>

<output>
After completion, create `.planning/phases/14-ui-ux-polish/14-06-SUMMARY.md`
</output>
