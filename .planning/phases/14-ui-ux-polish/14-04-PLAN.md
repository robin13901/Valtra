---
phase: 14-ui-ux-polish
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-03"]
files_modified:
  - lib/screens/electricity_screen.dart
  - lib/screens/gas_screen.dart
  - lib/screens/water_screen.dart
  - lib/screens/heating_screen.dart
  - lib/screens/smart_plugs_screen.dart
  - lib/screens/smart_plug_consumption_screen.dart
  - lib/screens/smart_plug_analytics_screen.dart
  - lib/screens/analytics_screen.dart
  - lib/screens/monthly_analytics_screen.dart
  - lib/screens/yearly_analytics_screen.dart
  - lib/providers/electricity_provider.dart
  - lib/providers/gas_provider.dart
  - lib/providers/heating_provider.dart
  - lib/providers/water_provider.dart
  - lib/widgets/charts/consumption_line_chart.dart
  - lib/widgets/charts/monthly_bar_chart.dart
  - lib/widgets/charts/year_comparison_chart.dart
  - lib/widgets/charts/consumption_pie_chart.dart
autonomous: true
requirements:
  - FR-12.2.1
  - FR-12.2.2
  - FR-12.2.3

must_haves:
  truths:
    - "All consumption values display using locale-aware formatting (DE: 1.234,5 / EN: 1,234.5)"
    - "Time values show 'Uhr' suffix in German locale (e.g., 9:43 Uhr)"
    - "Month names are localized (DE: Marz, EN: March)"
    - "Chart tooltips and axis labels use locale-aware formatting"
    - "No hardcoded NumberFormat('...', 'en') patterns remain in source code"
  artifacts:
    - path: "lib/screens/electricity_screen.dart"
      provides: "Locale-aware number formatting for electricity"
      contains: "ValtraNumberFormat"
    - path: "lib/widgets/charts/monthly_bar_chart.dart"
      provides: "Locale-aware chart tooltip formatting"
      contains: "ValtraNumberFormat"
  key_links:
    - from: "lib/screens/*.dart"
      to: "lib/services/number_format_service.dart"
      via: "ValtraNumberFormat.consumption/waterReading/currency calls"
      pattern: "ValtraNumberFormat\\."
    - from: "lib/screens/*.dart"
      to: "lib/providers/locale_provider.dart"
      via: "context.watch<LocaleProvider>().localeString"
      pattern: "LocaleProvider.*localeString"
---

<objective>
Replace all hardcoded English number formatting with locale-aware ValtraNumberFormat calls across all screens, providers, and chart widgets. Wire locale from LocaleProvider into every formatting call.

Purpose: FR-12.2.1 requires German number format everywhere (comma decimal, dot thousands). Currently 16+ locations use `NumberFormat('#,##0.0', 'en')` and many more use `toStringAsFixed()`. This plan cascades the centralized ValtraNumberFormat utility from Plan 01 to every display location.

Output: 18 modified files with locale-aware formatting. Zero hardcoded 'en' locale patterns remaining.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-ui-ux-polish/14-RESEARCH.md
@.planning/phases/14-ui-ux-polish/14-01-SUMMARY.md

@lib/services/number_format_service.dart
@lib/providers/locale_provider.dart
</context>

<interfaces>
<!-- From Plan 01 outputs -->

```dart
class ValtraNumberFormat {
  static String consumption(double value, String locale);    // 1 decimal
  static String waterReading(double value, String locale);   // 3 decimals
  static String currency(double value, String locale);       // 2 decimals
  static String time(DateTime dt, String locale);            // "9:43 Uhr" / "09:43"
  static String date(DateTime dt, String locale);            // "7. Marz 2026" / "March 7, 2026"
  static String monthYear(DateTime dt, String locale);       // "Marz 2026" / "March 2026"
}

class LocaleProvider extends ChangeNotifier {
  String get localeString;  // 'de' or 'en'
}
```

Usage pattern in screens:
```dart
final locale = context.watch<LocaleProvider>().localeString;
// Then:
ValtraNumberFormat.consumption(value, locale)
```

Usage pattern in chart widgets (no context.watch -- pass locale as parameter):
```dart
class MonthlyBarChart extends StatelessWidget {
  final String locale;  // Add this parameter
  // Use ValtraNumberFormat.consumption(value, locale) in tooltip builders
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace number formatting in all meter screens and providers</name>
  <files>
    lib/screens/electricity_screen.dart,
    lib/screens/gas_screen.dart,
    lib/screens/water_screen.dart,
    lib/screens/heating_screen.dart,
    lib/screens/smart_plugs_screen.dart,
    lib/screens/smart_plug_consumption_screen.dart,
    lib/providers/electricity_provider.dart,
    lib/providers/gas_provider.dart,
    lib/providers/heating_provider.dart,
    lib/providers/water_provider.dart
  </files>
  <action>
    **Screens (6 files):**
    For each screen file:
    1. Import `number_format_service.dart` and `locale_provider.dart`
    2. In the build method, get locale: `final locale = context.watch<LocaleProvider>().localeString;`
    3. Replace every `NumberFormat('#,##0.0', 'en').format(value)` with `ValtraNumberFormat.consumption(value, locale)`
    4. Replace every `NumberFormat('#,##0.000', 'en').format(value)` with `ValtraNumberFormat.waterReading(value, locale)` (water screen)
    5. Replace every `value.toStringAsFixed(1)` with `ValtraNumberFormat.consumption(value, locale)`
    6. Replace any time formatting with `ValtraNumberFormat.time(dateTime, locale)`
    7. Replace any date/month formatting with `ValtraNumberFormat.monthYear(dateTime, locale)` or `ValtraNumberFormat.date(dateTime, locale)`

    Specific locations from research:
    - electricity_screen.dart: line ~218 (NumberFormat)
    - gas_screen.dart: line ~220 (NumberFormat)
    - heating_screen.dart: lines ~119, ~251 (NumberFormat)
    - water_screen.dart: lines ~158, ~284 (NumberFormat with 3 decimals -> waterReading)
    - smart_plugs_screen.dart: line ~229 (NumberFormat)
    - smart_plug_consumption_screen.dart: line ~233 (NumberFormat)

    **Providers (4 files):**
    The providers use NumberFormat for formatting error messages (e.g., "Value must be >= 1234.5").
    Per research recommendation: providers should return raw numbers, and screens format them.
    1. In each provider, find where NumberFormat is used for display purposes
    2. Replace with returning raw double values
    3. Let the screen layer format using ValtraNumberFormat
    4. If the provider creates a user-facing string with a number, change it to use a placeholder pattern:
       - Before: `'Must be >= ${NumberFormat(...).format(value)}'`
       - After: Return the raw value, let the screen format it
    5. Remove `import 'package:intl/intl.dart';` from providers if no longer needed
  </action>
  <verify>
    <automated>flutter analyze --no-pub && flutter test --no-pub</automated>
  </verify>
  <done>
    6 meter screens use ValtraNumberFormat for all number display.
    4 providers return raw values instead of formatted strings.
    No `NumberFormat('#,##0', 'en')` patterns remain in these 10 files.
    All tests pass, flutter analyze clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace number formatting in analytics screens and chart widgets</name>
  <files>
    lib/screens/analytics_screen.dart,
    lib/screens/monthly_analytics_screen.dart,
    lib/screens/yearly_analytics_screen.dart,
    lib/screens/smart_plug_analytics_screen.dart,
    lib/widgets/charts/consumption_line_chart.dart,
    lib/widgets/charts/monthly_bar_chart.dart,
    lib/widgets/charts/year_comparison_chart.dart,
    lib/widgets/charts/consumption_pie_chart.dart
  </files>
  <action>
    **Analytics screens (4 files):**
    1. Import `number_format_service.dart` and `locale_provider.dart`
    2. Get locale: `final locale = context.watch<LocaleProvider>().localeString;`
    3. Replace all `toStringAsFixed(1)` calls with `ValtraNumberFormat.consumption(value, locale)`
    4. Replace all `toStringAsFixed(2)` calls with `ValtraNumberFormat.currency(value, locale)` (cost values)
    5. Pass `locale` to chart widgets as a constructor parameter

    Specific locations from research:
    - analytics_screen.dart: lines ~239, ~242 (toStringAsFixed)
    - monthly_analytics_screen.dart: lines ~258, ~268 (toStringAsFixed)
    - yearly_analytics_screen.dart: lines ~267, ~283, ~299 (toStringAsFixed)
    - smart_plug_analytics_screen.dart: lines ~379, ~385, ~396, ~463, ~491 (toStringAsFixed)

    **Chart widgets (4 files):**
    1. Add `final String locale;` parameter to each chart widget constructor
    2. Import `number_format_service.dart`
    3. In tooltip builders (FL Chart's `TouchedSpotData`, `BarTouchTooltipData`, etc.):
       - Replace `value.toStringAsFixed(1)` with `ValtraNumberFormat.consumption(value, locale)`
    4. In axis label builders:
       - Replace hardcoded number formatting with `ValtraNumberFormat.consumption(value, locale)`
    5. In consumption_pie_chart.dart: percentage labels stay as-is (e.g., "45.2%") or use locale-aware formatting

    **Update all callers** of chart widgets to pass the locale parameter:
    - MonthlyBarChart(locale: locale, ...)
    - YearComparisonChart(locale: locale, ...)
    - ConsumptionLineChart(locale: locale, ...)
    - ConsumptionPieChart(locale: locale, ...)
  </action>
  <verify>
    <automated>flutter analyze --no-pub && flutter test --no-pub</automated>
  </verify>
  <done>
    4 analytics screens use ValtraNumberFormat for all number display.
    4 chart widgets accept locale parameter and format tooltips/labels locale-aware.
    No `toStringAsFixed` or `NumberFormat('...', 'en')` patterns remain in any screen or chart file.
    All tests pass, flutter analyze clean.
  </done>
</task>

</tasks>

<verification>
- `flutter test --no-pub` — all tests pass
- `flutter analyze --no-pub` — zero issues
- Grep for `NumberFormat.*'en'` in lib/ returns zero results
- Grep for `toStringAsFixed` in lib/screens/ and lib/widgets/charts/ returns zero results (or only in non-display contexts)
</verification>

<success_criteria>
- All consumption/water/cost values formatted via ValtraNumberFormat
- German locale shows comma decimal, dot thousands (1.234,5)
- English locale shows dot decimal, comma thousands (1,234.5)
- Time shows "Uhr" suffix in German
- Month names localized
- Chart tooltips and axis labels locale-aware
- No hardcoded 'en' locale in NumberFormat calls
</success_criteria>

<output>
After completion, create `.planning/phases/14-ui-ux-polish/14-04-SUMMARY.md`
</output>
