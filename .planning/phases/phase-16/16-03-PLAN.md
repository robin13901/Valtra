---
phase: 16-backup-testing-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/**/*_test.dart
autonomous: true
requirements: [NFR-10.1, NFR-10.3, NFR-10.5]

must_haves:
  truths:
    - "Overall statement coverage is 80% or higher"
    - "All 855+ existing tests continue to pass"
    - "No new test introduces flakiness or platform dependencies"
    - "Coverage gaps in screens, widgets, and error paths are filled"
  artifacts:
    - path: "coverage/lcov.info"
      provides: "Coverage report data"
    - path: "test/"
      provides: "Test files filling coverage gaps"
  key_links:
    - from: "test/"
      to: "lib/"
      via: "test imports matching source files"
      pattern: "import.*package:valtra"
---

<objective>
Analyze test coverage and fill gaps to achieve 80%+ statement coverage across the codebase.

Purpose: NFR-10.1 requires 80%+ statement coverage. With 855 tests across 66 files, there are likely gaps in screen tests, error handling paths, and edge cases. This plan identifies and fills those gaps.
Output: Additional test cases in existing and new test files, achieving 80%+ coverage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@test/helpers/test_database.dart
@test/helpers/test_utils.dart
@test/helpers/test_locale_provider.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run coverage analysis and identify gaps</name>
  <files>coverage/lcov.info</files>
  <action>
1. Run `flutter test --coverage` to generate `coverage/lcov.info`
2. Analyze the lcov.info file to identify files below 80% coverage
3. Categorize gaps by priority:
   - **High priority**: Service files, DAO files, provider files below 80%
   - **Medium priority**: Screen files, widget files below 80%
   - **Low priority**: Generated files, theme files (already excluded)

4. For each file below 80%, identify the specific uncovered lines/branches:
   - Error handling catch blocks
   - Null/empty state branches
   - Edge case conditions
   - UI state variations (loading, error, empty)

5. Create a prioritized list of test additions needed, focusing on:
   - Files with the lowest coverage first
   - Error paths that are easy to test
   - Branch conditions that are simple to trigger

Note: Files matching these patterns are typically excluded from coverage and should be ignored: `*.g.dart`, `app_theme.dart`, `tables.dart`, `l10n/*`

If `lcov` or `genhtml` is not available for detailed per-file analysis, parse the `lcov.info` file directly:
- Lines starting with `SF:` indicate source files
- `LH:` = lines hit, `LF:` = lines found
- Coverage % = LH / LF * 100
  </action>
  <verify>
    <automated>cd C:\SAPDevelop\Privat\Valtra && flutter test --coverage && head -5 coverage/lcov.info</automated>
  </verify>
  <done>
    - coverage/lcov.info file generated
    - Gap analysis complete with prioritized list of files needing additional tests
  </done>
</task>

<task type="auto">
  <name>Task 2: Fill coverage gaps with targeted tests</name>
  <files>test/**/*_test.dart</files>
  <action>
Based on the gap analysis from Task 1, add targeted tests. Common gap areas in this codebase (based on project history):

**Likely screen test gaps** (screens often have the lowest coverage):
- `lib/screens/smart_plugs_screen.dart` -- If no dedicated test file exists at `test/screens/smart_plugs_screen_test.dart`, create one
- `lib/screens/heating_screen.dart` -- Test heating type toggle, room assignment display
- Error states and empty states in all screen tests
- Dialog interactions (delete confirmation, form submission)

**Likely provider gap areas:**
- Error handling in async operations (try/catch branches)
- Edge cases: empty lists, null household IDs, concurrent operations
- `CostConfigProvider` edge cases (no configs, expired configs)

**Likely widget gap areas:**
- `lib/widgets/liquid_glass_widgets.dart` -- GlassCard, GlassBottomNav, buildGlassAppBar, buildGlassFAB
- `lib/widgets/household_selector.dart` -- Dropdown interactions
- Chart widgets in `lib/widgets/charts/`

**Test writing approach:**
1. For each gap file, read the source to understand untested branches
2. Write focused tests for uncovered branches (not duplicating existing tests)
3. Use existing test helpers: `createTestDatabase()`, `MockLocaleProvider`, `wrapWithProviders()`
4. For screen tests, follow the existing pattern from `settings_screen_test.dart`:
   - Create mock providers with mocktail
   - Wrap in MultiProvider + MaterialApp with localization delegates
   - Use `locale: const Locale('en')` for English test assertions
5. For provider tests, follow existing patterns: create real in-memory DB, real DAOs, test async operations

**Test helpers to potentially create:**
- If many screen tests need the same mock setup, add shared helpers to `test/helpers/`
- Mock for common providers if not already mocked

**Key constraint:** Do NOT modify any source files in `lib/`. Only add/modify test files. All 855+ existing tests must continue to pass.

**Target:** After this task, re-run `flutter test --coverage` and verify overall coverage >= 80%. If specific files are still below 80% and are non-trivial to test (e.g., platform-specific code), document them as acceptable exceptions.
  </action>
  <verify>
    <automated>cd C:\SAPDevelop\Privat\Valtra && flutter test --coverage && flutter test && flutter analyze</automated>
  </verify>
  <done>
    - Coverage report shows 80%+ overall statement coverage
    - All existing 855+ tests still pass
    - New tests cover previously uncovered error paths, edge cases, and UI states
    - `flutter analyze` reports zero issues
    - No flaky tests introduced
  </done>
</task>

</tasks>

<verification>
```bash
cd C:\SAPDevelop\Privat\Valtra
flutter test --coverage
flutter test
flutter analyze
# Parse coverage summary
grep -c "^DA:" coverage/lcov.info  # total lines
grep "^DA:.*,0$" coverage/lcov.info | wc -l  # uncovered lines
```
</verification>

<success_criteria>
- Overall statement coverage >= 80% (measured by lcov.info)
- All existing 855+ tests pass without modification
- New tests are focused and non-flaky
- `flutter analyze` reports zero issues
- Gap analysis documented (which files were below 80%, what was added)
</success_criteria>

<output>
After completion, create `.planning/phases/phase-16/16-03-SUMMARY.md`
Include: coverage percentage before/after, number of new tests added, files with remaining gaps and reasons.
</output>
