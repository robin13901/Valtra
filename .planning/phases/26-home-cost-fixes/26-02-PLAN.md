---
phase: 26-home-cost-fixes
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - lib/database/tables.dart
  - lib/screens/household_cost_settings_screen.dart
  - lib/widgets/dialogs/cost_profile_form_dialog.dart
  - lib/screens/heating_screen.dart
  - lib/providers/analytics_provider.dart
  - test/screens/household_cost_settings_screen_test.dart
  - test/screens/heating_screen_test.dart
  - test/widgets/dialogs/cost_profile_form_dialog_test.dart
autonomous: true

must_haves:
  truths:
    - "CostMeterType enum has exactly 3 values: electricity, gas, water -- no heating"
    - "Cost settings screen shows exactly 3 meter type cards (Electricity, Gas, Water) -- no Heating card"
    - "Heating analysis page has no kWh/EUR cost toggle icon in the app bar"
    - "Heating analysis page passes showCosts: false to all chart widgets (consumption-only)"
    - "AnalyticsProvider returns null for MeterType.heating in _toCostMeterType"
  artifacts:
    - path: "lib/database/tables.dart"
      provides: "CostMeterType enum without heating"
      contains: "enum CostMeterType { electricity, gas, water }"
    - path: "lib/screens/heating_screen.dart"
      provides: "Heating screen without cost toggle"
    - path: "lib/providers/analytics_provider.dart"
      provides: "Analytics provider returning null for heating cost type"
      contains: "return null"
  key_links:
    - from: "lib/providers/analytics_provider.dart"
      to: "lib/database/tables.dart"
      via: "_toCostMeterType returns null for MeterType.heating"
      pattern: "case MeterType\\.heating.*null"
    - from: "lib/screens/heating_screen.dart"
      to: "chart widgets"
      via: "showCosts always false, no toggle"
      pattern: "showCosts: false"
---

<objective>
Remove heating from CostMeterType enum and all downstream cost-related code in the heating screen. Heating meters are unitless proportional counters -- they should never have cost profiles or a cost toggle.

Purpose: Addresses COST-04 (remove CostMeterType.heating from enum) and COST-05 (remove kWh/EUR toggle from heating analysis page).

Output: Clean separation of heating from cost tracking, with all cascading switch cases and UI elements removed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-home-cost-fixes/26-RESEARCH.md
@.planning/phases/26-home-cost-fixes/26-01-SUMMARY.md

@lib/database/tables.dart
@lib/screens/household_cost_settings_screen.dart
@lib/widgets/dialogs/cost_profile_form_dialog.dart
@lib/screens/heating_screen.dart
@lib/providers/analytics_provider.dart
@test/screens/household_cost_settings_screen_test.dart
@test/screens/heating_screen_test.dart
@test/widgets/dialogs/cost_profile_form_dialog_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove CostMeterType.heating from enum and all switch cases</name>
  <files>
    lib/database/tables.dart
    lib/screens/household_cost_settings_screen.dart
    lib/widgets/dialogs/cost_profile_form_dialog.dart
    lib/providers/analytics_provider.dart
    test/screens/household_cost_settings_screen_test.dart
    test/widgets/dialogs/cost_profile_form_dialog_test.dart
  </files>
  <action>
    **COST-04: Remove `heating` from CostMeterType enum.**

    This is the highest-risk change in the phase due to cascading effects. Work through each file systematically:

    **1. `lib/database/tables.dart` (~line 101):**
    Change:
    ```dart
    enum CostMeterType { electricity, gas, water, heating }
    ```
    to:
    ```dart
    enum CostMeterType { electricity, gas, water }
    ```
    This is DB-safe because `heating` was the last ordinal (3) and no CostConfig rows exist with `meterType=3` in production.

    **2. `lib/screens/household_cost_settings_screen.dart`:**
    Remove the `case CostMeterType.heating:` entries from both switch statements:
    - In `_meterTypeLabel()` (~line 60-61): delete `case CostMeterType.heating: return l10n.heating;`
    - In `_meterTypeIcon()` (~line 73-74): delete `case CostMeterType.heating: return Icons.thermostat;`

    **3. `lib/widgets/dialogs/cost_profile_form_dialog.dart`:**
    In `_unitSuffix()` (~lines 89-98), remove the `case CostMeterType.heating:` line that falls through to electricity/gas. The remaining switch should be:
    ```dart
    String _unitSuffix(AppLocalizations l10n) {
      switch (widget.meterType) {
        case CostMeterType.electricity:
        case CostMeterType.gas:
          return l10n.pricePerKwh;
        case CostMeterType.water:
          return l10n.pricePerCubicMeter;
      }
    }
    ```

    **4. `lib/providers/analytics_provider.dart`:**
    In `_toCostMeterType()` (~lines 344-355), change the `MeterType.heating` case to return `null` instead of `CostMeterType.heating`:
    ```dart
    case MeterType.heating:
      return null;
    ```
    This ensures no cost calculation is attempted for heating meters anywhere in the analytics pipeline.

    **5. `test/screens/household_cost_settings_screen_test.dart`:**
    - The test at ~line 108-116 ("renders one card per meter type") asserts `expect(find.text('Heating'), findsOneWidget)`. REMOVE this assertion -- the cost settings screen should now show only 3 cards (Electricity, Gas, Water).
    - Search for any other test that references `CostMeterType.heating` or expects 4 cards, and update to 3.

    **6. `test/widgets/dialogs/cost_profile_form_dialog_test.dart`:**
    - Search for any test using `CostMeterType.heating` and remove or update. If the dialog buildDialog helper uses `CostMeterType.heating`, change to `CostMeterType.electricity`.
  </action>
  <verify>
    Run `flutter analyze` -- zero issues (confirms all switch cases are exhaustive without heating).
    Run `flutter test test/screens/household_cost_settings_screen_test.dart` -- passes.
    Run `flutter test test/widgets/dialogs/cost_profile_form_dialog_test.dart` -- passes.
    Run `flutter test test/providers/analytics_provider_test.dart` -- passes (if exists).
  </verify>
  <done>
    CostMeterType enum has exactly 3 values. All switch statements compile cleanly. Cost settings screen renders 3 cards. Analytics provider returns null for heating cost type. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove cost toggle and showCosts from heating screen</name>
  <files>
    lib/screens/heating_screen.dart
    test/screens/heating_screen_test.dart
  </files>
  <action>
    **COST-05: Remove kWh/EUR cost toggle from heating analysis page.**

    In `lib/screens/heating_screen.dart`:

    1. **Remove state variable** (~line 35): Delete `bool _showCosts = false;`

    2. **Remove cost toggle from app bar** (~line 81): Delete `if (_currentTab == 0) _buildCostToggle(context, l10n),`

    3. **Delete `_buildCostToggle` method** (~lines 120-132): Remove the entire method. After COST-04, `CostMeterType.heating` no longer exists, so this method would not compile anyway.

    4. **Remove all `showCosts` pass-throughs** in `_buildAnalyseContent()`:
       - `_YearlySummaryCard`: Change `showCosts: _showCosts` to `showCosts: false` (line 213)
       - `MonthlyBarChart`: Change `showCosts: _showCosts` to `showCosts: false` (line 228). Change `periodCosts: _showCosts ? data.monthlyCosts : null` to `periodCosts: null` (line 229). Change `costUnit: _showCosts ? ... : null` to `costUnit: null` (line 230).
       - `YearComparisonChart`: Change `showCosts: _showCosts` to `showCosts: false` (line 249). Change `currentYearCosts: _showCosts ? ... : null` to `currentYearCosts: null` (line 250). Change `previousYearCosts: _showCosts ? ... : null` to `previousYearCosts: null` (line 252). Change `costUnit: _showCosts ? ... : null` to `costUnit: null` (line 254).

    5. **Remove unused imports**: After removing `_buildCostToggle`, check if `CostConfigProvider` and `CostMeterType` imports are still needed in this file. The `CostConfigProvider` import at line 9 and `tables.dart` import at line 6 may become unused -- remove if so. Run `flutter analyze` to confirm.

    In `test/screens/heating_screen_test.dart`:

    6. **Delete the cost toggle test group** (~line 619 "HeatingScreen - Cost Toggle on Analyse Tab"): Remove all tests in this group since the cost toggle no longer exists.

    7. **Update the existing test** (~line 532 "cost toggle hidden when no cost config"): Remove this test entirely since there is no cost toggle at all now.

    8. **Add a new test**: `'no cost toggle visible on Analyse tab'` that navigates to the Analyse tab and verifies `find.byIcon(Icons.euro)` finds nothing AND `find.byIcon(Icons.thermostat)` in the app bar area finds nothing (thermostat icon should only appear in meter list items, not as a toggle button).
  </action>
  <verify>
    Run `flutter test test/screens/heating_screen_test.dart` -- all tests pass.
    Run `flutter analyze lib/screens/heating_screen.dart` -- zero issues, no unused imports.
  </verify>
  <done>
    Heating screen has no cost toggle button, no _showCosts state, and passes showCosts: false to all chart widgets. All chart widgets render consumption-only data for heating. Related tests removed and replaced with assertion confirming no cost toggle exists.
  </done>
</task>

</tasks>

<verification>
1. `flutter test` -- all project tests pass (no regressions)
2. `flutter analyze` -- zero issues across entire project
3. Grep confirms heating removed from enum: `grep "CostMeterType" lib/database/tables.dart` shows `{ electricity, gas, water }` only
4. Grep confirms no _showCosts in heating screen: `grep "_showCosts\|_buildCostToggle" lib/screens/heating_screen.dart` -- no results
5. Grep confirms null return for heating: `grep -A1 "MeterType.heating" lib/providers/analytics_provider.dart` shows `return null`
6. Grep confirms no CostMeterType.heating anywhere: `grep -r "CostMeterType.heating" lib/` -- no results
</verification>

<success_criteria>
- COST-04: CostMeterType enum is `{ electricity, gas, water }` -- no heating value. All switch statements compile. Analytics returns null for heating cost. Cost settings shows 3 cards.
- COST-05: HeatingScreen has no `_showCosts` state, no `_buildCostToggle` method, no cost toggle icon in app bar. Charts receive `showCosts: false` and `periodCosts: null`.
- No compile errors from removed enum value (all switch cases exhaustive)
- All tests pass including updated cost settings tests and new no-cost-toggle assertion
</success_criteria>

<output>
After completion, create `.planning/phases/26-home-cost-fixes/26-02-SUMMARY.md`
</output>
