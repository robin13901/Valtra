---
phase: 25-chart-localization-labels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/widgets/charts/monthly_bar_chart.dart
  - lib/widgets/charts/year_comparison_chart.dart
  - test/widgets/charts/monthly_bar_chart_test.dart
  - test/widgets/charts/year_comparison_chart_test.dart
autonomous: true

must_haves:
  truths:
    - "X-axis month labels show German abbreviations when locale='de' (Jan, Feb, Mar, Apr, Mai, Jun, Jul, Aug, Sep, Okt, Nov, Dez)"
    - "X-axis month labels show English abbreviations when locale='en' (Jan, Feb, Mar, Apr, May, Jun, Jul, Aug, Sep, Oct, Nov, Dec)"
    - "Tooltip month names are locale-aware (e.g. yMMM shows 'Mar 2025' in EN, 'März 2025' in DE)"
    - "Y-axis shows unit label (kWh, m3) in consumption mode"
    - "Y-axis shows currency symbol (EUR) in cost mode"
    - "MonthlyBarChart and YearComparisonChart both have locale-aware axes"
  artifacts:
    - path: "lib/widgets/charts/monthly_bar_chart.dart"
      provides: "Locale-aware DateFormat calls and Y-axis unit label"
      contains: "DateFormat.MMM(locale)"
    - path: "lib/widgets/charts/year_comparison_chart.dart"
      provides: "Locale-aware DateFormat calls and Y-axis unit label"
      contains: "DateFormat.MMM(locale)"
    - path: "test/widgets/charts/monthly_bar_chart_test.dart"
      provides: "Tests for locale-aware month labels and Y-axis unit"
      contains: "locale"
    - path: "test/widgets/charts/year_comparison_chart_test.dart"
      provides: "Tests for locale-aware month labels and Y-axis unit"
      contains: "locale"
  key_links:
    - from: "lib/widgets/charts/monthly_bar_chart.dart"
      to: "intl DateFormat"
      via: "locale parameter passed to DateFormat factory"
      pattern: "DateFormat\\.MMM\\(locale\\)"
    - from: "lib/widgets/charts/year_comparison_chart.dart"
      to: "intl DateFormat"
      via: "locale parameter passed to DateFormat factory"
      pattern: "DateFormat\\.MMM\\(locale\\)"
    - from: "lib/widgets/charts/monthly_bar_chart.dart"
      to: "fl_chart AxisTitles"
      via: "axisNameWidget showing displayUnit"
      pattern: "axisNameWidget"
    - from: "lib/widgets/charts/year_comparison_chart.dart"
      to: "fl_chart AxisTitles"
      via: "axisNameWidget showing displayUnit"
      pattern: "axisNameWidget"
---

<objective>
Add locale-aware month abbreviations to chart X-axes and unit/currency labels to chart Y-axes for MonthlyBarChart and YearComparisonChart.

Purpose: Fulfills CHART-01 (localized month names), CHART-02 (Y-axis unit labels), and CHART-03 (applied to Strom, Gas, Wasser -- no screen changes needed since screens already pass locale/unit/costUnit to chart widgets).

Output: Two chart widgets updated with locale-aware DateFormat calls and Y-axis axisNameWidget; comprehensive tests for both.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-chart-localization-labels/25-RESEARCH.md
@lib/widgets/charts/monthly_bar_chart.dart
@lib/widgets/charts/year_comparison_chart.dart
@test/widgets/charts/monthly_bar_chart_test.dart
@test/widgets/charts/year_comparison_chart_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Localize MonthlyBarChart X-axis + tooltips and add Y-axis unit label</name>
  <files>
    lib/widgets/charts/monthly_bar_chart.dart
    test/widgets/charts/monthly_bar_chart_test.dart
  </files>
  <action>
    In `monthly_bar_chart.dart`, make these specific changes:

    1. **X-axis locale fix (line 134):** Change `DateFormat.MMM().format(periods[index].periodStart)` to `DateFormat.MMM(locale).format(periods[index].periodStart)`.

    2. **Tooltip locale fix (line 107):** Change `DateFormat.yMMM().format(period.periodStart)` to `DateFormat.yMMM(locale).format(period.periodStart)`.

    3. **Y-axis unit label:** Add `axisNameWidget` to the existing `leftTitles: AxisTitles(...)` block. Compute the display unit at the top of `_buildTitles()`:
       ```dart
       final displayUnit = showCosts && costUnit != null ? costUnit! : unit;
       ```
       Then set on the leftTitles AxisTitles:
       ```dart
       leftTitles: AxisTitles(
         axisNameWidget: Text(
           displayUnit,
           style: Theme.of(context).textTheme.bodySmall,
         ),
         axisNameSize: 18,
         sideTitles: SideTitles(
           showTitles: true,
           reservedSize: 50,
           getTitlesWidget: ... // existing code unchanged
         ),
       ),
       ```

    In `monthly_bar_chart_test.dart`, add these new tests:

    4. **Update `buildTestWidget` helper** to accept an optional `locale` parameter (String, default 'de') and pass it to both the `MonthlyBarChart(locale: locale)` and the `MaterialApp(locale: Locale(locale))`. Then add a **Locale parameter test group**:
       - Test: `locale='de' renders German month abbreviations on X-axis` -- Build chart with locale='de', extract BarChartData, call getTitlesWidget for index 0 (Jan period), verify the SideTitleWidget contains a Text widget with 'Jan'. Then test index 2 (Mar period), verify it shows 'Mrz' (German for March). Use `tester.widget<Text>(find.descendant(of: find.byType(SideTitleWidget), matching: find.byType(Text)).first)` pattern.
       - Test: `locale='en' renders English month abbreviations on X-axis` -- Same approach, verify March shows 'Mar' (English).
       - Test: `Y-axis shows unit label in consumption mode` -- Build with unit='kWh', showCosts=false. Find Text widget containing 'kWh' that is part of the chart's left axis area.
       - Test: `Y-axis shows costUnit label in cost mode` -- Build with showCosts=true, costUnit='EUR'. Verify 'EUR' text appears.
       - Test: `Y-axis shows unit when showCosts=true but costUnit is null` -- Verify fallback to unit.

    **Important:** The test `buildTestWidget` helper must include `locale: const Locale('de')` or `locale: const Locale('en')` on MaterialApp to ensure `DateFormat` uses the correct locale data. Also add `initializeDateFormatting` import from `package:intl/date_symbol_data_local.dart` in test setup if needed.
  </action>
  <verify>
    Run `flutter test test/widgets/charts/monthly_bar_chart_test.dart` -- all tests pass.
    Run `flutter analyze lib/widgets/charts/monthly_bar_chart.dart` -- zero issues.
  </verify>
  <done>
    MonthlyBarChart X-axis labels and tooltips use locale-aware DateFormat; Y-axis displays unit or currency label via axisNameWidget; all existing and new tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Localize YearComparisonChart X-axis + tooltips and add Y-axis unit label</name>
  <files>
    lib/widgets/charts/year_comparison_chart.dart
    test/widgets/charts/year_comparison_chart_test.dart
  </files>
  <action>
    In `year_comparison_chart.dart`, make these specific changes:

    1. **X-axis locale fix (line 235):** Change `DateFormat.MMM().format(DateTime(2024, index + 1))` to `DateFormat.MMM(locale).format(DateTime(2024, index + 1))`.

    2. **Tooltip locale fix (line 198):** Change `DateFormat.MMM().format(DateTime(2024, monthIndex + 1))` to `DateFormat.MMM(locale).format(DateTime(2024, monthIndex + 1))`.

    3. **Y-axis unit label:** The `displayUnit` variable already exists at line 124. Add `axisNameWidget` to the existing `leftTitles: AxisTitles(...)` block:
       ```dart
       leftTitles: AxisTitles(
         axisNameWidget: Text(
           displayUnit,
           style: Theme.of(context).textTheme.bodySmall,
         ),
         axisNameSize: 18,
         sideTitles: SideTitles(
           showTitles: true,
           reservedSize: 50,
           getTitlesWidget: ... // existing code unchanged
         ),
       ),
       ```

    In `year_comparison_chart_test.dart`, add these new tests:

    4. **Locale parameter test group:** Update `_wrap` helper to accept optional `locale` parameter (default Locale('en') as currently):
       - Test: `locale='de' renders German month abbreviations on X-axis` -- Build chart with German locale, extract LineChartData, call getTitlesWidget for index 2 (March), verify shows 'Mrz'.
       - Test: `locale='en' renders English month abbreviations on X-axis` -- Same for English, March shows 'Mar'.
       - Test: `Y-axis shows unit label in consumption mode` -- Build with unit='kWh', verify 'kWh' Text exists.
       - Test: `Y-axis shows costUnit label in cost mode` -- Build with showCosts=true, costUnit='EUR', verify 'EUR' Text exists.
       - Test: `Y-axis shows unit when costUnit is null in cost mode` -- Verify fallback.

    **Important:** Update `_wrap` to pass `locale:` to MaterialApp and also pass `locale:` to the YearComparisonChart widget so chart DateFormat calls use the right locale. Add `initializeDateFormatting` if needed for German locale data in tests.
  </action>
  <verify>
    Run `flutter test test/widgets/charts/year_comparison_chart_test.dart` -- all tests pass.
    Run `flutter analyze lib/widgets/charts/year_comparison_chart.dart` -- zero issues.
  </verify>
  <done>
    YearComparisonChart X-axis labels and tooltips use locale-aware DateFormat; Y-axis displays unit or currency label via axisNameWidget; all existing and new tests pass.
  </done>
</task>

</tasks>

<verification>
1. `flutter test` -- all 1094+ tests pass (no regressions)
2. `flutter analyze` -- zero issues across entire project
3. Grep confirms no remaining locale-less DateFormat calls in chart files:
   `grep -n "DateFormat\." lib/widgets/charts/monthly_bar_chart.dart lib/widgets/charts/year_comparison_chart.dart` -- every DateFormat call includes locale parameter
4. Grep confirms axisNameWidget present in both chart files:
   `grep -n "axisNameWidget" lib/widgets/charts/monthly_bar_chart.dart lib/widgets/charts/year_comparison_chart.dart` -- both files contain axisNameWidget
</verification>

<success_criteria>
- CHART-01: DateFormat.MMM(locale) and DateFormat.yMMM(locale) used in both chart widgets; German locale produces Mrz/Okt/Dez; English locale produces Mar/Oct/Dec
- CHART-02: Both charts show unit (kWh, m3) or currency (EUR) on Y-axis via axisNameWidget
- CHART-03: No screen changes needed (verified: electricity, gas, water screens already pass locale, unit, showCosts, costUnit to all chart widgets)
- All existing tests pass with no regressions
- New tests cover locale-specific month rendering and Y-axis unit display
</success_criteria>

<output>
After completion, create `.planning/phases/25-chart-localization-labels/25-01-SUMMARY.md`
</output>
